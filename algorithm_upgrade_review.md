# Algorithm Upgrade Review

## 总判断

当前项目已经不是“缺算法”，而是“高级内容分散、主线不够收束”。从数模竞赛高水平提交标准看，现有代码的主要问题不在于没有复杂模型，而在于：

1. `RQ1` 预测部分仍偏规则递推，缺少矩阵表达、等价性验证与参数敏感性，论文里容易显得“算出来了但没验证”。
2. `RQ2` 实际上已经具备离散设施选址 + 容量约束 + 多目标评价的雏形，但论文若只写“枚举/MILP 跑出来了”，数学含金量会被低估。
3. `RQ3` 已经有站点—服务项目级扩搜、固定点、Pareto 和稳定性扩展，是全项目最强的算法部分，但需要更清楚地解释“为什么联合可行难找、卡在哪些站点和哪类边界约束”。
4. `RQ4` 已经有情景重算、敏感性与鲁棒性框架，值得进正文，但应避免把所有指标都堆进主线。

## RQ1 现有算法审查

### 当前主算法

- 三状态离散时间递推：
  - 统一死亡率 `0.05`
  - 新增老人比例 `0.07`
  - 单向转移 `自理 -> 半失能 -> 失能`
- 理论需求：`人数 × 月均需求次数`
- 消费约束需求：按“小区 × 老人类型”预算上限缩放收费服务需求

### 是否符合题意

- 硬约束总体符合。
- 风险点：当前代码对紧急救助采用“公益免费，不缩放”的解释分支，代码是自洽的，但论文必须明确口径。

### 数学含金量评价

- 作为主模型足够稳健。
- 作为国奖级论文表述仍偏弱，因为缺：
  - 状态转移矩阵表达；
  - 递推与矩阵形式等价性说明；
  - 关键参数局部敏感性。

### 最可能被评委质疑的点

- “这只是按规则往后推，没有验证吗？”
- “为什么新增老人都进入自理状态？”
- “消费约束缩放对需求的影响有没有量化解释？”

### 是否值得升级

- 值得。
- 但不值得引入 ARIMA、LSTM、灰色预测等重型时间序列模型，因为题目没有历史时间序列，强上只会削弱可信度。

### 推荐升级路径

- 升级为“状态转移矩阵 + 递推等价性验证 + 局部参数敏感性”的扩展层。

### 是否影响已有结果

- 不影响主结果。
- 只补充解释力与可验证性。

### 是否建议作为主模型

- 建议作为主模型的强化表述写进正文。

## RQ2 现有算法审查

### 当前主算法

- 基于 10 个离散小区 × 4 种建站状态（不建/小/中/大）的有限方案搜索。
- 配合一个带公平权重与容量安全因子的 MILP 版本。
- 指标层面已输出覆盖、可及绩效、容量安全、年度财务等。

### 是否符合题意

- 候选站点、预算、半径、容量、满意度分段、折旧与逐站财务等主约束整体已落实。
- 需要持续用约束审计脚本防止口径漂移。

### 数学含金量评价

- 实际上已经高于普通“暴力枚举”。
- 如果包装得当，可写成：
  - 有限离散精确搜索；
  - 多目标容量设施选址；
  - 枚举与 MILP 的双重验证框架。

### 最可能被评委质疑的点

- “为什么直接枚举，是否显得不高级？”
- “MILP 与枚举的关系是什么，是两个互相矛盾的模型吗？”
- “公平和安全是如何被纳入目标/约束的？”

### 是否值得升级

- 值得。
- 但不值得再引入遗传算法、模拟退火、粒子群等启发式元算法。当前候选空间有限，精确法更有说服力。

### 推荐升级路径

- 把当前实现升级表述为：
  - 有限离散精确搜索；
  - Pareto/epsilon-constraint 代表方案分析；
  - 容量瓶颈与安全阈值 trade-off 解释。

### 是否影响已有结果

- 不必影响主结果。
- 作为扩展分析即可。

### 是否建议作为主模型

- 主模型仍保持现有离散精确搜索 + MILP 校验。
- Pareto 与 epsilon-constraint 建议进入正文结果分析。

## RQ3 现有算法审查

### 当前主算法

- 已有站点级统一溢价主链路。
- 已新增站点—服务项目级候选定价扩搜、全局固定点复算、逐站利润率验算。
- 已有 Pareto 报告、稳定性扩展和 expanded search。

### 是否符合题意

- 在当前新版 `3_5` 系列中，逐站利润率、单站补贴上限、紧急救助免费、固定布局输入等硬约束基本对齐题意。
- 关键要求仍是：不能把未收敛或不满足逐站利润率的方案写成 `joint feasible`。

### 数学含金量评价

- 全项目最有竞争力的部分。
- 其真正价值不在“定价复杂”，而在：
  - 满意度—需求—补贴—利润率闭环；
  - 固定点求解；
  - 逐站利润率硬约束；
  - 可行域诊断。

### 最可能被评委质疑的点

- “为什么找不到联合可行解？”
- “是不是用区域总利润率偷偷替代了逐站利润率？”
- “服务级定价是不是过度拟合？是否可解释？”

### 是否值得升级

- 值得，但要克制。
- 真正应该做的是“诊断和解释”，不是再引入更重的复杂算法。

### 推荐升级路径

- 保持现有服务级离散定价和固定点框架；
- 增强：
  - 逐站绑定约束诊断；
  - 近可行方案解释；
  - 哪些站点卡在亏损下界，哪些卡在 8% 上界。

### 是否影响已有结果

- 不改主结果，只补诊断输出。

### 是否建议作为主模型

- 建议作为正文核心创新。

## RQ4 现有算法审查

### 当前主算法

- 四类情景重算；
- 输出情景总表、敏感性系数、鲁棒性指标、Monte Carlo 摘要。

### 是否符合题意

- 当前设计路径合理。
- 风险在于论文若不说明“哪些情景复用了 RQ1，为什么可复用”，容易被质疑偷换口径。

### 数学含金量评价

- 足够进入正文。
- 但不应把所有鲁棒性指标都塞进主线，应选最有解释力的 2 到 4 个。

### 最可能被评委质疑的点

- “情景是重算还是复用旧结果？”
- “敏感性系数如何定义？”
- “鲁棒性是布局稳定、容量安全还是财务合规？”

### 是否值得升级

- 主要是叙事升级，不需要再加更复杂算法。

### 推荐升级路径

- 保留当前参数扰动重算；
- 正文聚焦：
  - 布局稳定性；
  - 覆盖/可及性稳定性；
  - 财务合规稳定性。

## 推荐升级排序

### P0

- 约束审计与口径一致性维护。
- 所有 `joint feasible` 判定必须严格使用逐站利润率和收敛条件。

### P1

- `RQ1` 状态转移矩阵验证与局部敏感性。
- `RQ2` Pareto / epsilon-constraint 多目标扩展。
- `RQ3` 联合可行性逐站绑定诊断。

### P2

- 图表增强：
  - RQ2 Pareto 前沿；
  - RQ3 固定点收敛图；
  - RQ4 鲁棒性热力图。

### P3

- 不建议当前实现：
  - 遗传算法、粒子群、模拟退火；
  - 黑箱机器学习预测；
  - 多层双重博弈或连续非线性定价大模型。

这些看起来高级，但对当前题目与数据规模并不增益，反而会削弱可解释性和可信度。
