# B题：嵌入式社区养老服务站的建设与优化问题

## 导航目录

- [问题背景](#问题背景)
- [附件数据说明](#附件数据说明)
- [基本假设](#基本假设)
- [问题要求](#问题要求)
- [问题1：未来五年老人数量与服务需求量预测](#问题1未来五年老人数量与服务需求量预测)
- [问题2：服务站选址与规模优化](#问题2服务站选址与规模优化)
- [问题3：服务定价与政府补贴优化](#问题3服务定价与政府补贴优化)
- [问题4：灵敏度分析与方案比较](#问题4灵敏度分析与方案比较)

---

## 问题背景

某城市成熟街道下辖10个连片小区，当地政府计划建设嵌入式社区养老服务站，为老龄人群提供助餐、日间照料、上门护理、康复理疗、助浴、紧急救助等一系列为民服务项目，解决老龄人群面临的困难。

## 附件数据说明

| 附件编号 | 内容说明                                                                              |
| -------- | ------------------------------------------------------------------------------------- |
| 附件1    | 小区基础数据：（1）人口与老人结构；（2）转移概率                                      |
| 附件2    | 服务需求数据：（1）每位老人月均服务需求次数；（2）服务营收及支出；（3）月服务消费上限 |
| 附件3    | 服务站建设与运营成本                                                                  |
| 附件4    | 小区间距离矩阵（单位：米）                                                            |
| 附件5    | 满意度评分规则                                                                        |

## 基本假设

- 老人只选择满意度最高的服务站
- 服务站服务能力与规模成正比
- 五年内人口结构与失能率相对平稳变化
- 当地工资、物价与补贴政策保持稳定

## 问题要求

根据所给数据，解决以下几个问题：

---

# 问题1：未来五年老人数量与服务需求量预测

## 1.1 任务1：老人数量递推预测模型

假设条件：

- 老年人年均自然死亡率为 **5%**
- 年均新增老年人（刚满60岁）占当前总老年人口的 **7%**
- 失能老人无法恢复为自理或半失能老人
- 半失能老人有一定概率转移成为失能老人，但不会自然恢复为自理老人
- 自理老人有一定概率转移成为半失能老人

**任务**：根据附件1中每个小区老人的当前数量及转移概率，建立未来5年的老人数量递推预测模型，预测第1年至第5年末每个小区各类老人的数量。

## 1.2 任务2：理论月需求次数预测

**任务**：根据1.1的预测结果及附件2中的数据，预测第5年末每个小区对各项服务（助餐、日间照料、上门护理、康复理疗、助浴、紧急救助）的理论月需求次数。

**要求**：

- 分自理、半失能、失能三类老人
- 单位：次/月，尽量取整
- 注：此处为理论需求总量，未考虑消费能力约束

## 1.3 任务3：消费约束下的月均服务需求次数预测

**任务**：根据附件1及附件2所给数据及消费约束，预测第5年末每个小区各类老人月均服务需求次数。

**要求**：

- 单位：次/月，尽量取整

---

## 总体建模思路

问题1本质上分为三层：

第一层是老人状态递推预测。每个小区的老人被分为自理、半失能、失能三类。每年老人会发生死亡、新增进入老年群体，以及健康状态转移。因此可以建立离散时间状态转移模型，预测未来5年各小区三类老人数量。

第二层是理论服务需求预测。根据第5年末每个小区三类老人数量，以及附件2中不同老人类型对不同服务的月均需求次数，计算各项养老服务的理论月需求总量。

第三层是消费约束下的有效需求预测。理论需求不考虑老人支付能力，但附件2给出了服务价格、消费上限等信息，因此需要在预算约束下对服务需求进行修正，得到更接近实际发生的月均服务需求次数。

---

## 符号定义

设共有 $10$ 个小区，编号为：

$$
i=1,2,\dots,10
$$

老人健康状态分为三类：

$$
k \in \{1,2,3\}
$$

其中：

$$
1=\text{自理老人}, \quad 2=\text{半失能老人}, \quad 3=\text{失能老人}
$$

养老服务类型共有6类：

$$
s \in \{1,2,3,4,5,6\}
$$

分别表示：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

设第 $t$ 年末第 $i$ 个小区三类老人数量向量为：

$$
\mathbf{N}_i(t)=
\begin{bmatrix}
N_{i,1}(t) \\
N_{i,2}(t) \\
N_{i,3}(t)
\end{bmatrix}
$$

其中 $t=0$ 表示当前年份，$t=1,2,\dots,5$ 表示未来第1年至第5年末。

设附件1中第 $i$ 个小区当前老人数量为：

$$
\mathbf{N}_i(0)=
\begin{bmatrix}
N_{i,1}(0) \\
N_{i,2}(0) \\
N_{i,3}(0)
\end{bmatrix}
$$

设自理老人转移为半失能老人的概率为：

$$
p_{i,12}
$$

半失能老人转移为失能老人的概率为：

$$
p_{i,23}
$$

根据题目假设，失能老人无法恢复为半失能或自理老人，半失能老人不会恢复为自理老人，自理老人也不会直接转移为失能老人。

老人年均自然死亡率为：

$$
d=5\%=0.05
$$

年均新增老年人占当前总老年人口比例为：

$$
g=7\%=0.07
$$

---

## 1.4 模型1：未来五年老人数量递推预测模型

### 状态转移关系

每一年内，三类老人的状态转移关系如下：

自理老人中，一部分仍为自理老人，一部分转移为半失能老人：

$$
\text{自理} \rightarrow \text{自理},\quad \text{自理} \rightarrow \text{半失能}
$$

半失能老人中，一部分仍为半失能老人，一部分转移为失能老人：

$$
\text{半失能} \rightarrow \text{半失能},\quad \text{半失能} \rightarrow \text{失能}
$$

失能老人只能继续保持失能状态，或因自然死亡退出系统：

$$
\text{失能} \rightarrow \text{失能}
$$

若先考虑状态转移，再统一考虑死亡，则第 $i$ 个小区的状态转移矩阵可写为：

$$
\mathbf{P}_i=
\begin{bmatrix}
1-p_{i,12} & 0 & 0 \\
p_{i,12} & 1-p_{i,23} & 0 \\
        0 & p_{i,23} & 1
\end{bmatrix}
$$

于是，在不考虑新增老人的情况下，转移后的老人数量为：

$$
\mathbf{P}_i \mathbf{N}_i(t)
$$

考虑死亡率 $d=0.05$ 后，存活人数为：

$$
(1-d)\mathbf{P}_i \mathbf{N}_i(t)
$$

### 新增老人处理方式

题目给出：年均新增老年人占当前总老年人口的 $7\%$。

第 $t$ 年末第 $i$ 个小区当前总老年人口为：

$$
T_i(t)=N_{i,1}(t)+N_{i,2}(t)+N_{i,3}(t)
$$

则下一年新增老年人数为：

$$
A_i(t)=gT_i(t)
$$

一般情况下，刚满60岁的新增老人应主要进入自理老人类别。因此可假设新增老人全部进入自理老人状态：

$$
\mathbf{A}_i(t)=
\begin{bmatrix}
A_i(t) \\
0 \\
0
\end{bmatrix}
=
\begin{bmatrix}
gT_i(t) \\
0 \\
0
\end{bmatrix}
$$

如果附件1给出了新增老人中自理、半失能、失能比例，也可以将新增老人按比例分配。若没有说明，采用“新增老人全部为自理老人”是较合理且便于解释的假设。

### 递推模型

因此，第 $i$ 个小区从第 $t$ 年末到第 $t+1$ 年末的递推模型为：

$$
\mathbf{N}_i(t+1)
=
(1-d)\mathbf{P}_i\mathbf{N}_i(t)
\quad - \mathbf{A}_i(t)
$$

即：

$$
\mathbf{N}_i(t+1)
=
0.95
\begin{bmatrix}
1-p_{i,12} & 0 & 0 \\
p_{i,12} & 1-p_{i,23} & 0 \\
        0 & p_{i,23} & 1
\end{bmatrix}
\mathbf{N}_i(t)
\quad - \begin{bmatrix}
0.07T_i(t) \\
0 \\
0
\end{bmatrix}
$$

展开后：

$$
N_{i,1}(t+1)
=
0.95(1-p_{i,12})N_{i,1}(t)
\quad - 0.07T_i(t)
$$

$$
N_{i,2}(t+1)
=
0.95p_{i,12}N_{i,1}(t)
\quad - 0.95(1-p_{i,23})N_{i,2}(t)
$$

$$
N_{i,3}(t+1)
=
0.95p_{i,23}N_{i,2}(t)
\quad - 0.95N_{i,3}(t)
$$

其中：

$$
T_i(t)=N_{i,1}(t)+N_{i,2}(t)+N_{i,3}(t)
$$

对 $t=0,1,2,3,4$ 进行递推，即可得到第1年至第5年末每个小区三类老人的预测数量。

### 取整处理

递推过程中可以保留小数，避免逐年取整造成累计误差。最终输出时再对人数进行四舍五入：

$$
\widehat{N}_{i,k}(t)=\operatorname{round}(N_{i,k}(t))
$$

论文中可以说明：为减少递推误差，模型计算过程保留小数，最终结果表按人数单位取整。

### 输出表结构

问题1.1建议输出如下表格。

| 小区  |    年份 | 自理老人数量 | 半失能老人数量 | 失能老人数量 | 老人总数 |
| ----- | ------: | -----------: | -------------: | -----------: | -------: |
| 小区1 | 第1年末 |              |                |              |          |
| 小区1 | 第2年末 |              |                |              |          |
| 小区1 | 第3年末 |              |                |              |          |
| 小区1 | 第4年末 |              |                |              |          |
| 小区1 | 第5年末 |              |                |              |          |
| 小区2 | 第1年末 |              |                |              |          |
| ...   |     ... |          ... |            ... |          ... |      ... |

---

## 1.5 模型2：理论月需求次数预测模型

### 服务需求参数

设附件2给出的第 $k$ 类老人对第 $s$ 项服务的月均需求次数为：

$$
r_{k,s}
$$

例如：

$$
r_{1,1}=\text{自理老人每月助餐需求次数}
$$

$$
r_{2,4}=\text{半失能老人每月康复理疗需求次数}
$$

$$
r_{3,5}=\text{失能老人每月助浴需求次数}
$$

第5年末第 $i$ 个小区第 $k$ 类老人数量为：

$$
N_{i,k}(5)
$$

则第5年末第 $i$ 个小区第 $s$ 项服务的理论月需求次数为：

$$
D_{i,s}^{\text{theory}}
=
\sum_{k=1}^{3} N_{i,k}(5)r_{k,s}
$$

如果要求分自理、半失能、失能三类老人，则每一类老人的服务需求为：

$$
D_{i,k,s}^{\text{theory}}
=
N_{i,k}(5)r_{k,s}
$$

总需求为：

$$
D_{i,s}^{\text{theory}}
=
D_{i,1,s}^{\text{theory}}
+ D_{i,2,s}^{\text{theory}}
+ D_{i,3,s}^{\text{theory}}
$$

### 矩阵形式

定义第5年末第 $i$ 个小区老人数量行向量为：

$$
\mathbf{N}_i(5)=
\begin{bmatrix}
N_{i,1}(5) & N_{i,2}(5) & N_{i,3}(5)
\end{bmatrix}
$$

定义月均服务需求矩阵为：

$$
\mathbf{R}=
\begin{bmatrix}
r_{1,1} & r_{1,2} & r_{1,3} & r_{1,4} & r_{1,5} & r_{1,6} \\
r_{2,1} & r_{2,2} & r_{2,3} & r_{2,4} & r_{2,5} & r_{2,6} \\
r_{3,1} & r_{3,2} & r_{3,3} & r_{3,4} & r_{3,5} & r_{3,6}
\end{bmatrix}
$$

则第 $i$ 个小区六类服务的理论月需求向量为：

$$
\mathbf{D}_i^{\text{theory}}
=
\mathbf{N}_i(5)\mathbf{R}
$$

其中：

$$
\mathbf{D}_i^{\text{theory}}
=
\begin{bmatrix}
D_{i,1}^{\text{theory}} &
D_{i,2}^{\text{theory}} &
D_{i,3}^{\text{theory}} &
D_{i,4}^{\text{theory}} &
D_{i,5}^{\text{theory}} &
D_{i,6}^{\text{theory}}
\end{bmatrix}
$$

### 输出表结构

建议同时输出“分类需求表”和“汇总需求表”。

分类需求表：

| 小区  | 老人类型 | 助餐 | 日间照料 | 上门护理 | 康复理疗 | 助浴 | 紧急救助 |
| ----- | -------- | ---: | -------: | -------: | -------: | ---: | -------: |
| 小区1 | 自理     |      |          |          |          |      |          |
| 小区1 | 半失能   |      |          |          |          |      |          |
| 小区1 | 失能     |      |          |          |          |      |          |
| 小区2 | 自理     |      |          |          |          |      |          |
| ...   | ...      |  ... |      ... |      ... |      ... |  ... |      ... |

汇总需求表：

| 小区  | 助餐 | 日间照料 | 上门护理 | 康复理疗 | 助浴 | 紧急救助 | 合计 |
| ----- | ---: | -------: | -------: | -------: | ---: | -------: | ---: |
| 小区1 |      |          |          |          |      |          |      |
| 小区2 |      |          |          |          |      |          |      |
| ...   |  ... |      ... |      ... |      ... |  ... |      ... |  ... |
| 合计  |      |          |          |          |      |          |      |

---

## 1.6 模型3：消费约束下的月均服务需求预测模型

### 建模思路

问题1.2得到的是“想要多少服务”，但问题1.3要预测“在消费上限约束下实际能够发生多少服务”。

附件2中通常会包含：

1. 各项服务单次收费或消费金额；
2. 不同类型老人月服务消费上限；
3. 不同类型老人对各服务的月均理论需求次数。

因此，需要在每类老人月消费上限下，对服务需求次数进行约束修正。

设第 $s$ 项服务的单次价格为：

$$
c_s
$$

设第 $k$ 类老人月服务消费上限为：

$$
B_k
$$

设第 $k$ 类老人对第 $s$ 项服务的理论月均需求次数为：

$$
r_{k,s}
$$

则第 $k$ 类老人若完全满足理论需求，其月均服务消费为：

$$
C_k^{\text{theory}}
=
\sum_{s=1}^{6} c_s r_{k,s}
$$

如果：

$$
C_k^{\text{theory}} \leq B_k
$$

说明该类老人理论需求没有超过消费上限，有效需求等于理论需求：

$$
r_{k,s}^{\text{eff}}=r_{k,s}
$$

如果：

$$
C_k^{\text{theory}} > B_k
$$

说明理论需求超过月消费能力，需要对服务需求次数进行压缩。

---

### 简化比例缩放模型

如果附件5或题目没有给出服务优先级，可以采用比例缩放法。该方法假设老人会按原有服务需求结构等比例减少各项服务消费。

定义消费约束修正系数：

$$
\lambda_k=
\min\left(
1,
\frac{B_k}{C_k^{\text{theory}}}
\right)
$$

则消费约束后的第 $k$ 类老人第 $s$ 项服务月均需求次数为：

$$
r_{k,s}^{\text{eff}}
=
\lambda_k r_{k,s}
$$

第 $i$ 个小区第 $k$ 类老人第 $s$ 项服务的有效月需求次数为：

$$
D_{i,k,s}^{\text{eff}}
=
N_{i,k}(5)r_{k,s}^{\text{eff}}
$$

即：

$$
D_{i,k,s}^{\text{eff}}
=
N_{i,k}(5)\lambda_k r_{k,s}
$$

第 $i$ 个小区第 $s$ 项服务的消费约束后总需求为：

$$
D_{i,s}^{\text{eff}}
=
\sum_{k=1}^{3}
N_{i,k}(5)\lambda_k r_{k,s}
$$

这个模型优点是简单、稳定、容易解释，适合数学建模报告中的基础预测部分。

---

### 优先级优化模型

如果希望模型更有“优化问题”的味道，可以把消费约束下的需求分配建成线性规划模型。

设 $x_{k,s}$ 表示第 $k$ 类老人对第 $s$ 项服务在消费约束下的月均实际消费次数。则有：

$$
0 \leq x_{k,s} \leq r_{k,s}
$$

消费约束为：

$$
\sum_{s=1}^{6} c_s x_{k,s} \leq B_k
$$

目标函数可以设置为最大化需求满足程度：

$$
\max \sum_{s=1}^{6} w_s \frac{x_{k,s}}{r_{k,s}}
$$

其中 $w_s$ 表示第 $s$ 项服务的重要性权重。

如果没有服务优先级数据，可以令：

$$
w_s=1
$$

如果题目或附件5中的满意度规则能够体现不同服务的重要性，可以根据满意度或服务必要性设置权重。例如：

$$
w_{\text{紧急救助}} > w_{\text{上门护理}} > w_{\text{助浴}} > w_{\text{康复理疗}} > w_{\text{日间照料}} > w_{\text{助餐}}
$$

也可以根据服务性质设置如下权重：

| 服务     | 建议权重 |
| -------- | -------: |
| 紧急救助 |     1.00 |
| 上门护理 |     0.90 |
| 康复理疗 |     0.80 |
| 助浴     |     0.75 |
| 日间照料 |     0.65 |
| 助餐     |     0.60 |

不过，如果附件没有明确依据，报告中不建议随意设置主观权重。更稳妥的做法是采用比例缩放模型，并在后续灵敏度分析中讨论不同服务优先级对结果的影响。

---

### 推荐采用的模型

对于问题1.3，我建议主模型采用比例缩放法：

$$
\lambda_k=
\min\left(
1,
\frac{B_k}{\sum_{s=1}^{6} c_s r_{k,s}}
\right)
$$

$$
D_{i,k,s}^{\text{eff}}
=
N_{i,k}(5)\lambda_k r_{k,s}
$$

$$
D_{i,s}^{\text{eff}}
=
\sum_{k=1}^{3}
D_{i,k,s}^{\text{eff}}
$$

原因是：

第一，该模型严格满足消费上限约束。

第二，该模型不会人为改变老人原有服务需求结构。

第三，参数全部来自附件2，不需要额外构造主观权重。

第四，结果稳定，便于与问题1.2的理论需求进行对比。

---

## 1.7 问题1整体算法流程

可以按照以下步骤求解。

步骤1，读取附件1中10个小区当前三类老人数量：

$$
N_{i,1}(0),N_{i,2}(0),N_{i,3}(0)
$$

以及状态转移概率：

$$
p_{i,12},p_{i,23}
$$

步骤2，对每个小区建立状态转移矩阵：

$$
\mathbf{P}_i=
\begin{bmatrix}
1-p_{i,12} & 0 & 0 \\
p_{i,12} & 1-p_{i,23} & 0 \\
        0 & p_{i,23} & 1
\end{bmatrix}
$$

步骤3，对 $t=0,1,2,3,4$ 递推：

$$
\mathbf{N}_i(t+1)
=
0.95\mathbf{P}_i\mathbf{N}_i(t)
\quad - \begin{bmatrix}
0.07T_i(t) \\
0 \\
0
\end{bmatrix}
$$

步骤4，得到第5年末各小区三类老人数量：

$$
N_{i,k}(5)
$$

步骤5，读取附件2中三类老人对六类服务的月均需求矩阵：

$$
\mathbf{R}
$$

步骤6，计算理论月需求：

$$
D_{i,k,s}^{\text{theory}}
=
N_{i,k}(5)r_{k,s}
$$

$$
D_{i,s}^{\text{theory}}
=
\sum_{k=1}^{3}D_{i,k,s}^{\text{theory}}
$$

步骤7，读取附件2中服务单价 $c_s$ 和不同老人类型月消费上限 $B_k$。

步骤8，计算每类老人的理论月消费：

$$
C_k^{\text{theory}}
=
\sum_{s=1}^{6} c_s r_{k,s}
$$

步骤9，计算消费约束修正系数：

$$
\lambda_k=
\min\left(
1,
\frac{B_k}{C_k^{\text{theory}}}
\right)
$$

步骤10，计算消费约束下的有效需求：

$$
D_{i,k,s}^{\text{eff}}
=
N_{i,k}(5)\lambda_k r_{k,s}
$$

$$
D_{i,s}^{\text{eff}}
=
\sum_{k=1}^{3}D_{i,k,s}^{\text{eff}}
$$

步骤11，最终结果取整输出。

---

## 1.8 可写入论文的模型表述

可以这样写：

本文将老人健康状态划分为自理、半失能和失能三类，并以小区为基本空间单元建立离散时间递推模型。考虑到老年群体在未来五年内受到自然死亡、新增老年人口和健康状态恶化三类因素影响，模型以年度为时间步长，对每个小区分别构造状态转移矩阵。根据题意，失能老人不再恢复为半失能或自理状态，半失能老人不会自然恢复为自理状态，自理老人仅可能转移为半失能状态。因此，健康状态转移呈单向递进结构。模型在每年转移后统一扣除5%的自然死亡率，并将相当于当前老年人口7%的新增老年人口计入自理老人群体，从而得到未来五年各小区三类老人的数量预测结果。

在服务需求预测中，本文首先根据第5年末各小区三类老人数量和附件2给出的月均服务需求次数，计算六类养老服务的理论月需求总量。该结果反映在不考虑消费能力约束条件下的潜在服务需求。进一步地，考虑到不同类型老人存在月服务消费上限，本文引入消费约束修正系数。当某类老人理论服务消费未超过消费上限时，其有效需求等于理论需求；当理论服务消费超过消费上限时，按照原服务需求结构进行等比例压缩。该方法能够在保持服务需求结构相对稳定的同时，保证预测结果满足消费能力约束。

---

## 1.9 Python求解框架

附件数据整理成表格后，可以用下面的代码框架计算。变量名可以根据附件列名修改。

```python
import numpy as np
import pandas as pd

# -----------------------------
# 1. 基础参数
# -----------------------------
death_rate = 0.05
new_rate = 0.07
years = 5

# 服务名称
services = ["助餐", "日间照料", "上门护理", "康复理疗", "助浴", "紧急救助"]

# 老人类型
types = ["自理", "半失能", "失能"]

# -----------------------------
# 2. 读取数据
# -----------------------------
# community_df 示例列：
# 小区, 自理老人, 半失能老人, 失能老人, p12, p23
community_df = pd.read_excel("附件1.xlsx")

# demand_df 示例：
# 行为老人类型，列为6类服务月均需求次数
# 老人类型, 助餐, 日间照料, 上门护理, 康复理疗, 助浴, 紧急救助
demand_df = pd.read_excel("附件2_需求.xlsx")

# price_df 示例：
# 服务, 单价
price_df = pd.read_excel("附件2_服务价格.xlsx")

# budget_df 示例：
# 老人类型, 月消费上限
budget_df = pd.read_excel("附件2_消费上限.xlsx")

# -----------------------------
# 3. 整理需求矩阵 R
# -----------------------------
demand_df = demand_df.set_index("老人类型")
R = demand_df.loc[types, services].values  # 3 x 6

# 服务单价向量
price_df = price_df.set_index("服务")
c = price_df.loc[services, "单价"].values  # 6

# 消费上限向量
budget_df = budget_df.set_index("老人类型")
B = budget_df.loc[types, "月消费上限"].values  # 3

# -----------------------------
# 4. 老人数量递推预测
# -----------------------------
pop_records = []

N5_dict = {}

for _, row in community_df.iterrows():
    community = row["小区"]

    N = np.array([
        row["自理老人"],
        row["半失能老人"],
        row["失能老人"]
    ], dtype=float)

    p12 = row["p12"]
    p23 = row["p23"]

    P = np.array([
        [1 - p12, 0, 0],
        [p12, 1 - p23, 0],
        [0, p23, 1]
    ], dtype=float)

    for t in range(1, years + 1):
        total_old = N.sum()
        A = np.array([new_rate * total_old, 0, 0], dtype=float)

        N = (1 - death_rate) * P @ N + A

        pop_records.append({
            "小区": community,
            "年份": f"第{t}年末",
            "自理老人数量": round(N[0]),
            "半失能老人数量": round(N[1]),
            "失能老人数量": round(N[2]),
            "老人总数": round(N.sum())
        })

    N5_dict[community] = N.copy()

pop_result = pd.DataFrame(pop_records)

# -----------------------------
# 5. 理论需求预测
# -----------------------------
theory_records = []
theory_summary_records = []

for community, N5 in N5_dict.items():
    # 分类需求：3 x 6
    D_by_type = N5.reshape(3, 1) * R

    for k_idx, elder_type in enumerate(types):
        record = {
            "小区": community,
            "老人类型": elder_type
        }
        for s_idx, service in enumerate(services):
            record[service] = round(D_by_type[k_idx, s_idx])
        theory_records.append(record)

    # 汇总需求：1 x 6
    D_total = D_by_type.sum(axis=0)

    summary_record = {
        "小区": community
    }
    for s_idx, service in enumerate(services):
        summary_record[service] = round(D_total[s_idx])
    summary_record["合计"] = round(D_total.sum())
    theory_summary_records.append(summary_record)

theory_by_type = pd.DataFrame(theory_records)
theory_summary = pd.DataFrame(theory_summary_records)

# -----------------------------
# 6. 消费约束下的有效需求预测
# -----------------------------
# 每类老人理论月消费
C_theory = R @ c  # 3

# 修正系数
lambda_k = np.minimum(1, B / C_theory)

# 修正后的月均需求矩阵
R_eff = R * lambda_k.reshape(3, 1)

eff_records = []
eff_summary_records = []

for community, N5 in N5_dict.items():
    D_eff_by_type = N5.reshape(3, 1) * R_eff

    for k_idx, elder_type in enumerate(types):
        record = {
            "小区": community,
            "老人类型": elder_type,
            "消费修正系数": round(lambda_k[k_idx], 4)
        }
        for s_idx, service in enumerate(services):
            record[service] = round(D_eff_by_type[k_idx, s_idx])
        eff_records.append(record)

    D_eff_total = D_eff_by_type.sum(axis=0)

    summary_record = {
        "小区": community
    }
    for s_idx, service in enumerate(services):
        summary_record[service] = round(D_eff_total[s_idx])
    summary_record["合计"] = round(D_eff_total.sum())
    eff_summary_records.append(summary_record)

eff_by_type = pd.DataFrame(eff_records)
eff_summary = pd.DataFrame(eff_summary_records)

# -----------------------------
# 7. 输出结果
# -----------------------------
with pd.ExcelWriter("问题1预测结果.xlsx") as writer:
    pop_result.to_excel(writer, sheet_name="1.1老人数量预测", index=False)
    theory_by_type.to_excel(writer, sheet_name="1.2理论需求_分类", index=False)
    theory_summary.to_excel(writer, sheet_name="1.2理论需求_汇总", index=False)
    eff_by_type.to_excel(writer, sheet_name="1.3消费约束需求_分类", index=False)
    eff_summary.to_excel(writer, sheet_name="1.3消费约束需求_汇总", index=False)

print("计算完成，结果已输出到：问题1预测结果.xlsx")
```

---

## 1.10 模型优缺点分析

该模型的主要优点是结构清晰，能够把人口递推、健康状态转移、服务需求预测和消费能力约束统一到一个可计算框架中。状态转移矩阵反映了老人健康状态从自理到半失能、从半失能到失能的单向变化规律；理论需求模型能够直观反映第5年末各小区的潜在服务需求；消费约束模型进一步修正了实际可实现的服务需求，使预测结果更符合现实消费能力。

该模型的局限在于，新增老人被统一假设为自理老人，可能低估新增老人中存在慢性病或轻度失能的情况；自然死亡率被设为三类老人相同，也可能低估失能老人死亡率、高估自理老人死亡率。此外，消费约束下采用比例缩放法虽然稳定，但没有体现不同服务之间的重要性差异。若后续需要提高模型精度，可以引入分类型死亡率、不同年龄段进入老年群体的健康分布，以及基于服务优先级的线性规划模型。

采用统一转移概率进行计算：

$$
p_{12}=4.5\%,\quad p_{23}=10\%
$$

即：

$$
\text{自理老人}\rightarrow \text{半失能老人}=4.5\%
$$

$$
\text{半失能老人}\rightarrow \text{失能老人}=10\%
$$

同时保留前面模型中的假设：

$$
d=5\%,\quad g=7\%
$$

其中 $d$ 为年自然死亡率，$g$ 为新增老年人比例。新增老年人默认全部进入自理老人类别。

---

### 递推模型代入形式

对任意小区 $i$，设第 $t$ 年末三类老人数量分别为：

$$
N_{i,1}(t),\quad N_{i,2}(t),\quad N_{i,3}(t)
$$

分别表示自理老人、半失能老人、失能老人数量。

总老人数量为：

$$
T_i(t)=N_{i,1}(t)+N_{i,2}(t)+N_{i,3}(t)
$$

代入题目数据后，递推公式为：

$$
N_{i,1}(t+1)
=
0.95\times(1-0.045)N_{i,1}(t)+0.07T_i(t)
$$

即：

$$
N_{i,1}(t+1)=0.90725N_{i,1}(t)+0.07T_i(t)
$$

$$
N_{i,2}(t+1)
=
0.95\times0.045N_{i,1}(t)+0.95\times(1-0.10)N_{i,2}(t)
$$

即：

$$
N_{i,2}(t+1)=0.04275N_{i,1}(t)+0.855N_{i,2}(t)
$$

$$
N_{i,3}(t+1)
=
0.95\times0.10N_{i,2}(t)+0.95N_{i,3}(t)
$$

即：

$$
N_{i,3}(t+1)=0.095N_{i,2}(t)+0.95N_{i,3}(t)
$$

计算过程保留小数，最终结果按人数四舍五入。

---

### 第1年至第5年末老人数量预测结果

| 小区 |    年份 | 自理老人 | 半失能老人 | 失能老人 | 老人总数 |
| ---- | ------: | -------: | ---------: | -------: | -------: |
| A    | 第1年末 |      500 |        151 |       75 |      726 |
| A    | 第2年末 |      504 |        151 |       86 |      741 |
| A    | 第3年末 |      509 |        150 |       96 |      756 |
| A    | 第4年末 |      515 |        150 |      105 |      771 |
| A    | 第5年末 |      521 |        151 |      114 |      786 |
| B    | 第1年末 |      413 |        134 |       74 |      620 |
| B    | 第2年末 |      418 |        132 |       83 |      633 |
| B    | 第3年末 |      423 |        131 |       91 |      645 |
| B    | 第4年末 |      429 |        130 |       99 |      658 |
| B    | 第5年末 |      436 |        129 |      106 |      671 |
| C    | 第1年末 |      638 |        205 |       96 |      938 |
| C    | 第2年末 |      644 |        202 |      110 |      957 |
| C    | 第3年末 |      652 |        201 |      124 |      976 |
| C    | 第4年末 |      659 |        199 |      137 |      996 |
| C    | 第5年末 |      668 |        199 |      149 |     1016 |
| D    | 第1年末 |      372 |        119 |       65 |      555 |
| D    | 第2年末 |      376 |        117 |       73 |      567 |
| D    | 第3年末 |      381 |        116 |       81 |      578 |
| D    | 第4年末 |      386 |        116 |       87 |      589 |
| D    | 第5年末 |      391 |        115 |       94 |      601 |
| E    | 第1年末 |      541 |        174 |       85 |      800 |
| E    | 第2年末 |      547 |        172 |       97 |      816 |
| E    | 第3年末 |      553 |        171 |      109 |      832 |
| E    | 第4年末 |      560 |        169 |      119 |      849 |
| E    | 第5年末 |      568 |        168 |      130 |      866 |
| F    | 第1年末 |      331 |        103 |       48 |      481 |
| F    | 第2年末 |      334 |        102 |       55 |      491 |
| F    | 第3年末 |      338 |        101 |       62 |      501 |
| F    | 第4年末 |      341 |        101 |       69 |      511 |
| F    | 第5年末 |      345 |        101 |       75 |      521 |
| G    | 第1年末 |      598 |        190 |       94 |      881 |
| G    | 第2年末 |      604 |        188 |      108 |      899 |
| G    | 第3年末 |      611 |        186 |      121 |      917 |
| G    | 第4年末 |      618 |        185 |      132 |      935 |
| G    | 第5年末 |      626 |        185 |      143 |      954 |
| H    | 第1年末 |      396 |        126 |       58 |      579 |
| H    | 第2年末 |      400 |        125 |       67 |      591 |
| H    | 第3年末 |      405 |        124 |       75 |      603 |
| H    | 第4年末 |      409 |        123 |       83 |      615 |
| H    | 第5年末 |      414 |        123 |       91 |      627 |
| I    | 第1年末 |      509 |        166 |       77 |      751 |
| I    | 第2年末 |      514 |        164 |       88 |      766 |
| I    | 第3年末 |      520 |        162 |       99 |      781 |
| I    | 第4年末 |      526 |        161 |      109 |      797 |
| I    | 第5年末 |      533 |        159 |      120 |      813 |
| J    | 第1年末 |      460 |        142 |       67 |      669 |
| J    | 第2年末 |      465 |        141 |       77 |      683 |
| J    | 第3年末 |      469 |        140 |       87 |      696 |
| J    | 第4年末 |      474 |        140 |       96 |      710 |
| J    | 第5年末 |      480 |        140 |      104 |      724 |

---

### 第5年末结果汇总

问题1.2和1.3后续只需要使用第5年末的结果。因此可以单独整理如下：

| 小区 | 第5年末自理老人 | 第5年末半失能老人 | 第5年末失能老人 | 第5年末老人总数 |
| ---- | --------------: | ----------------: | --------------: | --------------: |
| A    |             521 |               151 |             114 |             786 |
| B    |             436 |               129 |             106 |             671 |
| C    |             668 |               199 |             149 |            1016 |
| D    |             391 |               115 |              94 |             601 |
| E    |             568 |               168 |             130 |             866 |
| F    |             345 |               101 |              75 |             521 |
| G    |             626 |               185 |             143 |             954 |
| H    |             414 |               123 |              91 |             627 |
| I    |             533 |               159 |             120 |             813 |
| J    |             480 |               140 |             104 |             724 |

合计结果为：

| 类别       | 第5年末数量 |
| ---------- | ----------: |
| 自理老人   |        4982 |
| 半失能老人 |        1470 |
| 失能老人   |        1226 |
| 老人总数   |        7678 |

---

### 结果解释

从预测结果看，10个小区第5年末老人总数由当前的6864人增加到约7678人，整体增长约814人。虽然每年存在5%的自然死亡率，但7%的新增老年人口比例高于死亡率，同时失能状态具有不可逆性，因此总老人规模总体上升，失能老人数量增长更明显。

其中，第5年末老人规模最大的三个小区是 C、G、E，分别约为1016人、954人和866人。这些小区在后续服务站选址与能力配置中应作为重点需求区域。失能老人数量较多的小区包括 C、G、E、I、A，这些区域对上门护理、助浴、康复理疗、紧急救助等服务的需求预计会更强。

---

### 问题1.2详细推导

#### 建模目标

问题1.2要求预测第5年末每个小区对各项养老服务的理论月需求次数。这里的“理论需求”只考虑老人数量和服务需求频次，不考虑收入水平、消费能力、服务站容量和服务距离。

也就是说，只要知道：

$$
第5年末某小区某类老人数量
$$

以及：

该类老人对某项服务的月均需求次数

就可以直接相乘得到该类老人的理论月需求总次数。

---

#### 老人类型与服务类型

老人类型分为三类：

$$
k=1,2,3
$$

其中：

$$
1=\text{自理老人}
$$

$$
2=\text{半失能老人}
$$

$$
3=\text{失能老人}
$$

服务类型共有六类：

$$
s=1,2,\dots,6
$$

分别为：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

---

#### 理论服务需求矩阵

月均服务需求矩阵构造如下：

$$
R=
\begin{bmatrix}
14 & 8 & 0 & 2 & 0 & 0.15 \\
20 & 14 & 6 & 4 & 2 & 1 \\
22 & 18 & 12 & 6 & 4 & 3
\end{bmatrix}
$$

其中，矩阵的行表示老人类型，列表示服务项目。

具体含义为：

$$
R_{k,s}=\text{第 }k\text{ 类老人对第 }s\text{ 项服务的月均需求次数}
$$

例如：

$$
R_{1,1}=14
$$

表示每位自理老人每月平均需要14次助餐服务。

$$
R_{3,3}=12
$$

表示每位失能老人每月平均需要12次上门护理服务。

---

#### 理论需求计算公式

设第5年末第 $i$ 个小区第 $k$ 类老人数量为：

$$
N_{i,k}^{(5)}
$$

则第 $i$ 个小区第 $k$ 类老人对第 $s$ 项服务的理论月需求次数为：

$$
D_{i,k,s}^{0}=N_{i,k}^{(5)}R_{k,s}
$$

其中，上标 $0$ 表示“未考虑消费约束的理论需求”。

第 $i$ 个小区第 $s$ 项服务的理论月需求总次数为：

$$
D_{i,s}^{0}=\sum_{k=1}^{3}D_{i,k,s}^{0}
$$

即：

$$
D_{i,s}^{0}
=
\sum_{k=1}^{3}N_{i,k}^{(5)}R_{k,s}
$$

---

#### 矩阵表达

设第 $i$ 个小区第5年末三类老人数量向量为：

$$
\mathbf{N}_i^{(5)}
=
\begin{bmatrix}
N_{i,1}^{(5)} & N_{i,2}^{(5)} & N_{i,3}^{(5)}
\end{bmatrix}
$$

则该小区六类服务的理论月需求向量为：

$$
\mathbf{D}_i^{0}
=
\mathbf{N}_i^{(5)}R
$$

展开为：

$$
\mathbf{D}_i^{0}
=
\begin{bmatrix}
D_{i,1}^{0} &
D_{i,2}^{0} &
D_{i,3}^{0} &
D_{i,4}^{0} &
D_{i,5}^{0} &
D_{i,6}^{0}
\end{bmatrix}
$$

---

### 问题1.3详细推导

#### 建模目标

问题1.3要求在消费能力约束下预测第5年末各小区各类老人的实际月均服务需求次数。

与问题1.2相比，问题1.3多了一个关键约束：

$$
\text{理论服务费用总额} \leq \text{月服务消费上限}
$$

如果某类老人的理论服务费用不超过消费上限，则该类老人保持原理论需求。

如果某类老人的理论服务费用超过消费上限，则需要按照题目要求：

等比例削减各类服务次数

---

### 消费上限模型

#### 小区收入参数

附件1已经给出了每个小区的人均月收入。

设第 $i$ 个小区人均月收入为：

$$
I_i
$$

例如：

$$
I_A=3400
$$

$$
I_B=3100
$$

$$
I_C=3800
$$

不同老人类型的月服务消费上限占月收入比例不同。

自理老人：

$$
\alpha_1=20\%=0.20
$$

半失能老人：

$$
\alpha_2=25\%=0.25
$$

失能老人：

$$
\alpha_3=30\%=0.30
$$

因此，第 $i$ 个小区第 $k$ 类老人的月服务消费上限为：

$$
B_{i,k}=\alpha_k I_i
$$

即：

$$
B_{i,1}=0.20I_i
$$

$$
B_{i,2}=0.25I_i
$$

$$
B_{i,3}=0.30I_i
$$

这里要注意：消费上限是“每位老人”的月服务消费上限，不是整个小区的消费上限。整个小区的上限可以由单人上限乘以对应老人数量得到，但在等比例削减时，最好先从“单个老人”的服务需求结构出发进行修正。

---

### 服务费用模型

#### 单次服务营收价格

问题1.3中的消费约束应使用“单次服务营收”，因为这是老人实际支付或形成消费额的价格。

设第 $s$ 项服务的单次营收价格为：

$$
p_s
$$

服务营收价格向量为：

$$
\mathbf{p}
=
\begin{bmatrix}
10 & 20 & 30 & 28 & 25 & 0
\end{bmatrix}
$$

其中紧急救助为公益免费服务，营收为0元。

对应服务顺序为：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

因此：

$$
p_1=10,\quad p_2=20,\quad p_3=30,\quad p_4=28,\quad p_5=25,\quad p_6=0
$$

---

#### 理论月服务消费额

第 $k$ 类老人若完全按照理论需求消费，其单人月理论服务费用为：

$$
C_k^0=\sum_{s=1}^{6}R_{k,s}p_s
$$

由于服务需求矩阵 $R$ 不随小区变化，服务单价也不随小区变化，所以三类老人理论单人月服务费用 $C_k^0$ 只与老人类型有关。

代入数据计算：

### 自理老人

$$
C_1^0
=
14\times10+8\times20+0\times30+2\times28+0\times25+0.15\times0
$$

$$
C_1^0=140+160+56=356
$$

所以自理老人单人月理论服务费用为：

$$
356\text{ 元/月}
$$

### 半失能老人

$$
C_2^0
=
20\times10+14\times20+6\times30+4\times28+2\times25+1\times0
$$

$$
C_2^0=200+280+180+112+50=822
$$

所以半失能老人单人月理论服务费用为：

$$
822\text{ 元/月}
$$

### 失能老人

$$
C_3^0
=
22\times10+18\times20+12\times30+6\times28+4\times25+3\times0
$$

$$
C_3^0=220+360+360+168+100=1208
$$

所以失能老人单人月理论服务费用为：

$$
1208\text{ 元/月}
$$

整理为：

| 老人类型   | 单人月理论服务费用 |
| ---------- | -----------------: |
| 自理老人   |          356 元/月 |
| 半失能老人 |          822 元/月 |
| 失能老人   |         1208 元/月 |

---

### 消费约束修正系数

#### 修正系数定义

对第 $i$ 个小区第 $k$ 类老人，定义消费约束修正系数：

$$
\lambda_{i,k}
=
\min\left(1,\frac{B_{i,k}}{C_k^0}\right)
$$

其中：

$$
B_{i,k}=\alpha_k I_i
$$

$$
C_k^0=\sum_{s=1}^{6}R_{k,s}p_s
$$

如果：

$$
B_{i,k}\geq C_k^0
$$

则：

$$
\lambda_{i,k}=1
$$

说明消费能力足以覆盖理论服务费用，不需要削减服务次数。

如果：

$$
B_{i,k}<C_k^0
$$

则：

$$
\lambda_{i,k}=\frac{B_{i,k}}{C_k^0}
$$

说明该类老人的服务需求需要按比例削减。

---

#### 修正后的单人月服务需求

消费约束后的第 $i$ 个小区第 $k$ 类老人对第 $s$ 项服务的月均需求次数为：

$$
R_{i,k,s}^{\ast}
=
\lambda_{i,k}R_{k,s}
$$

其中，上标 $\ast$ 表示消费约束修正后的需求。

---

#### 修正后的总服务需求

第 $i$ 个小区第 $k$ 类老人对第 $s$ 项服务的消费约束后月需求总次数为：

$$
D_{i,k,s}^{\ast}
=
N_{i,k}^{(5)}R_{i,k,s}^{\ast}
$$

代入可得：

$$
D_{i,k,s}^{\ast}
=
N_{i,k}^{(5)}\lambda_{i,k}R_{k,s}
$$

第 $i$ 个小区第 $s$ 项服务的消费约束后总需求为：

$$
D_{i,s}^{\ast}
=
\sum_{k=1}^{3}D_{i,k,s}^{\ast}
$$

即：

$$
D_{i,s}^{\ast}
=
\sum_{k=1}^{3}N_{i,k}^{(5)}\lambda_{i,k}R_{k,s}
$$

---

### 是否需要考虑紧急救助的消费约束

紧急救助的单次营收为0元，因此它不占用老人月消费预算。

也就是说，在计算理论服务费用时：

$$
p_6=0
$$

所以紧急救助不会导致消费额上升。

但是题目说“若某类老人的理论服务费用总额超过消费上限，则等比例削减各类服务次数”。严格按这句话理解，一旦总费用超限，六类服务次数都要按照同一个 $\lambda_{i,k}$ 削减，包括紧急救助。

这里有两个建模选择。

#### 选择一：严格按题意削减全部服务

若某类老人费用超限，则：

$$
R_{i,k,s}^{\ast}
=
\lambda_{i,k}R_{k,s},\quad s=1,\dots,6
$$

这种做法完全符合题面“各类服务次数等比例削减”的描述。优点是模型统一，计算简单，适合竞赛题或课程作业。

#### 选择二：紧急救助不削减

由于紧急救助是公益免费服务，理论上不受消费能力限制。也可以设：

$$
R_{i,k,6}^{\ast}=R_{k,6}
$$

而其他收费服务按比例削减：

$$
R_{i,k,s}^{\ast}
=
\lambda_{i,k}R_{k,s},\quad s=1,\dots,5
$$

这种做法更符合现实逻辑，但与题目“等比例削减各类服务次数”的字面要求略有偏差。

建议在本题中采用选择一，即严格按题意对六类服务统一等比例削减。这样更稳妥，便于说明。

---

### 消费约束是否会真正起作用

各小区人均月收入在：

$$
2700\sim 3800
$$

之间。

三类老人消费上限分别为：

自理老人：

$$
0.20I_i
$$

半失能老人：

$$
0.25I_i
$$

失能老人：

$$
0.30I_i
$$

以最低收入小区 F 为例：

$$
I_F=2700
$$

自理老人消费上限：

$$
B_{F,1}=0.20\times2700=540
$$

半失能老人消费上限：

$$
B_{F,2}=0.25\times2700=675
$$

失能老人消费上限：

$$
B_{F,3}=0.30\times2700=810
$$

与理论服务费用比较：

| 老人类型   | 理论费用 | F小区消费上限 | 是否超限 |
| ---------- | -------: | ------------: | -------- |
| 自理老人   |      356 |           540 | 不超限   |
| 半失能老人 |      822 |           675 | 超限     |
| 失能老人   |     1208 |           810 | 超限     |

这说明消费约束主要影响半失能老人和失能老人，对自理老人基本不产生约束。

再看最高收入小区 C：

$$
I_C=3800
$$

自理老人消费上限：

$$
B_{C,1}=760
$$

半失能老人消费上限：

$$
B_{C,2}=950
$$

失能老人消费上限：

$$
B_{C,3}=1140
$$

与理论服务费用比较：

| 老人类型   | 理论费用 | C小区消费上限 | 是否超限 |
| ---------- | -------: | ------------: | -------- |
| 自理老人   |      356 |           760 | 不超限   |
| 半失能老人 |      822 |           950 | 不超限   |
| 失能老人   |     1208 |          1140 | 超限     |

可见，即使在收入最高的小区，失能老人的理论服务费用仍然可能超过消费上限。因此，消费约束下需求预测的重点在失能老人群体，其次是低收入小区中的半失能老人群体。

---

### 服务营收与支出数据在后续问题中的作用

"服务营收及支出"数据不仅用于问题1.3中的消费约束，还会在后续服务站建设和运营优化中发挥作用。

#### 单次营收

单次营收用于计算老人消费金额，也可用于计算服务站营业收入：

$$
Revenue_{i,s}=D_{i,s}^{\ast}p_s
$$

若后续考虑服务站分配，则某个服务站 $j$ 的服务收入为：

$$
Revenue_j=\sum_{i}\sum_{s}x_{i,j,s}p_s
$$

其中 $x_{i,j,s}$ 表示小区 $i$ 分配给服务站 $j$ 的第 $s$ 类服务次数。

#### 单次直接支出

单次直接支出用于计算服务站的服务变动成本：

$$
Cost_{i,s}^{direct}=D_{i,s}^{\ast}q_s
$$

其中 $q_s$ 表示第 $s$ 项服务的单次直接支出。

服务直接支出向量为：

$$
\mathbf{q}
=
\begin{bmatrix}
8 & 16 & 24 & 23 & 20 & 8
\end{bmatrix}
$$

对应：

| 服务     | 单次直接支出 |
| -------- | -----------: |
| 助餐     |            8 |
| 日间照料 |           16 |
| 上门护理 |           24 |
| 康复理疗 |           23 |
| 助浴     |           20 |
| 紧急救助 |            8 |

#### 单次毛收益

单次毛收益可以表示为：

$$
m_s=p_s-q_s
$$

代入数据：

| 服务     | 单次营收 | 单次直接支出 | 单次毛收益 |
| -------- | -------: | -----------: | ---------: |
| 助餐     |       10 |            8 |          2 |
| 日间照料 |       20 |           16 |          4 |
| 上门护理 |       30 |           24 |          6 |
| 康复理疗 |       28 |           23 |          5 |
| 助浴     |       25 |           20 |          5 |
| 紧急救助 |        0 |            8 |         -8 |

紧急救助由于公益免费，单次毛收益为负。这一点在后续运营成本、政府补贴、公益服务覆盖约束中很重要。

---

### 问题1.3推荐的最终模型

为了和题目要求保持一致，建议最终采用如下模型：

第 $i$ 个小区第 $k$ 类老人的消费上限为：

$$
B_{i,k}=\alpha_k I_i
$$

其中：

$$
\alpha_1=0.20,\quad \alpha_2=0.25,\quad \alpha_3=0.30
$$

第 $k$ 类老人的理论月服务费用为：

$$
C_k^0=\sum_{s=1}^{6}R_{k,s}p_s
$$

消费修正系数为：

$$
\lambda_{i,k}
=
\min\left(1,\frac{B_{i,k}}{C_k^0}\right)
$$

消费约束后的单人月均服务需求为：

$$
R_{i,k,s}^{\ast}
=
\lambda_{i,k}R_{k,s}
$$

消费约束后的第 $i$ 个小区第 $k$ 类老人第 $s$ 项服务月需求次数为：

$$
D_{i,k,s}^{\ast}
=
N_{i,k}^{(5)}R_{i,k,s}^{\ast}
$$

即：

$$
D_{i,k,s}^{\ast}
=
N_{i,k}^{(5)}\lambda_{i,k}R_{k,s}
$$

第 $i$ 个小区第 $s$ 项服务的消费约束后总需求为：

$$
D_{i,s}^{\ast}
=
\sum_{k=1}^{3}N_{i,k}^{(5)}\lambda_{i,k}R_{k,s}
$$

---

### 后续集中求解时的计算步骤

后续集中求解时，可以按下面顺序执行：

第一步，使用问题1.1得到的第5年末老人数量：

$$
N_{i,1}^{(5)},N_{i,2}^{(5)},N_{i,3}^{(5)}
$$

第二步，构造服务需求矩阵：

$$
R=
\begin{bmatrix}
14 & 8 & 0 & 2 & 0 & 0.15 \\
20 & 14 & 6 & 4 & 2 & 1 \\
22 & 18 & 12 & 6 & 4 & 3
\end{bmatrix}
$$

第三步，构造服务营收价格向量：

$$
\mathbf{p}
=
\begin{bmatrix}
10 & 20 & 30 & 28 & 25 & 0
\end{bmatrix}
$$

第四步，计算三类老人理论月服务费用：

$$
C^0=
\begin{bmatrix}
356 & 822 & 1208
\end{bmatrix}
$$

第五步，根据每个小区人均月收入 $I_i$ 计算三类消费上限：

$$
B_{i,1}=0.20I_i
$$

$$
B_{i,2}=0.25I_i
$$

$$
B_{i,3}=0.30I_i
$$

第六步，计算消费约束修正系数：

$$
\lambda_{i,k}
=
\min\left(1,\frac{B_{i,k}}{C_k^0}\right)
$$

第七步，计算理论需求：

$$
D_{i,k,s}^{0}=N_{i,k}^{(5)}R_{k,s}
$$

第八步，计算消费约束需求：

$$
D_{i,k,s}^{\ast}=N_{i,k}^{(5)}\lambda_{i,k}R_{k,s}
$$

第九步，按照题目要求对次数取整。

建议最终同时输出四张表：

第一张：第5年末老人数量表。

第二张：理论月需求次数表，按“小区—服务项目”汇总。

第三张：消费修正系数表，按“小区—老人类型”展示。

第四张：消费约束下月需求次数表，按“小区—服务项目”汇总。

---

### 可写入论文的表述

可以直接写成如下文字：

在理论需求预测阶段，本文以第5年末各小区三类老人数量为基础，结合不同健康状态老人对助餐、日间照料、上门护理、康复理疗、助浴和紧急救助六类服务的月均需求频次，构建服务需求矩阵。通过老人数量向量与服务需求矩阵相乘，得到各小区六类养老服务的理论月需求次数。该结果反映了不受支付能力约束时的潜在服务规模。

在消费约束修正阶段，本文进一步考虑老人服务消费能力。根据附件数据，三类老人的月服务消费上限分别取所在小区人均月收入的20%、25%和30%。本文首先根据各类服务单次营收计算三类老人理论月服务费用，再将其与对应消费上限进行比较。当理论费用不超过消费上限时，服务需求保持不变；当理论费用超过消费上限时，按照题目要求对该类老人各项服务次数进行等比例削减。由此得到消费约束下的有效服务需求。该处理方式既保留了不同老人类型原有的服务需求结构，又保证预测结果满足收入约束，能够更真实地反映实际可实现的服务规模。

---

# 问题2：服务站选址与规模优化

基于问题1预测的第5年末每个小区老人数量与消费约束的服务需求量，考虑服务站选址与规模优化问题。

## 2.0 相关要求说明

### 服务站规模

| 规模类型 | 服务能力上限 |
| -------- | ------------ |
| 小型     | ≤1000人次/日 |
| 中型     | ≤2000人次/日 |
| 大型     | ≤3000人次/日 |

> 对应建设成本与运营成本见附件3

### 服务站可选位置

- 10个小区中的任意若干个（仅限小区内部）
- 小区间距离见附件4

### 服务半径要求

- 有效服务半径 ≤ **1000米**
- 超出距离不产生有效服务需求

### 服务覆盖率

- 至少享受一项养老服务的老年人数占老年总人口的比例

### 服务满意度

- 根据附件5的评分规则（距离、服务响应、价格）计算
- 取值范围：**0.6 ~ 1**

### 实际有效服务人次

$$
\text{实际有效服务人次} = \text{理论需求人次} \times \text{服务满意度}
$$

## 2.1 优化模型建立

**约束条件**：

- 总建设预算不超过 **120万元**
- 服务站建设需满足上述所有要求

**目标**：

- 确定区域内需建设服务站数量、位置、规模
- 使得服务覆盖率和服务满意度尽可能高

## 2.2 求解算法设计

**任务**：

- 设计求解算法
- 说明算法的主要步骤
- 分析算法的时间复杂度

## 2.3 结果分析与模型评价

**任务**：

- 给出最优的站点数量、位置（哪个小区）、规模（大/中/小）
- 给出每个站点覆盖的小区
- 计算每个服务站预计年度利润
- 计算服务覆盖率
- 计算每个小区老人满意度
- 指出至少3点模型的局限性
- 提出至少一种改进方向

---

下面给出问题2的建模求解方案。它可以直接作为报告中“服务站选址与规模优化”部分的基础框架。后续只要补充附件4的小区距离矩阵、附件5的满意度评分规则，以及问题1.3得到的消费约束后服务需求量，就可以集中求解具体最优站点方案。

---

### 问题本质分析

问题2属于典型的设施选址与容量配置优化问题。政府可以在10个小区中选择若干个小区建设养老服务站，每个服务站可以选择小型、中型或大型规模。不同规模对应不同建设成本、固定管理成本和服务能力上限。建站方案需要满足建设预算不超过120万元、服务半径不超过1000米、服务能力不能超过站点容量等约束。

同时，该问题不是单纯追求成本最小，而是希望在预算约束下，使服务覆盖率和服务满意度尽可能高。因此，它具有多目标优化特征。可将“服务覆盖率”和“服务满意度”合成为综合效益目标，也可以采用分层优化策略：先最大化覆盖率，再在覆盖率最优或接近最优的方案中最大化满意度。

考虑到本题候选站点只有10个，规模类型只有3种，问题规模较小，完全可以采用枚举法、整数规划法或启发式搜索法求解。其中，枚举法最适合课程设计和数学建模报告，因为它逻辑透明、结果可复现、不会依赖复杂黑箱算法。

---

### 符号定义

设小区集合为：

$$
I=\{1,2,\dots,10\}
$$

候选服务站位置集合与小区集合相同：

$$
J=\{1,2,\dots,10\}
$$

服务站规模集合为：

$$
K=\{S,M,L\}
$$

其中：

$$
S=\text{小型},\quad M=\text{中型},\quad L=\text{大型}
$$

服务类型集合为：

$$
R=\{1,2,\dots,6\}
$$

分别表示：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

设第 $i$ 个小区第5年末老人总数为：

$$
P_i
$$

设第 $i$ 个小区在消费约束后的月服务需求总人次为：

$$
D_i
$$

如果按服务类型区分，则第 $i$ 个小区第 $r$ 类服务的月需求人次为：

$$
D_{i,r}
$$

第 $i$ 个小区的日均服务需求人次可由月需求折算得到：

$$
d_i=\frac{D_i}{30}
$$

若按服务类型区分：

$$
d_{i,r}=\frac{D_{i,r}}{30}
$$

设第 $i$ 个小区与第 $j$ 个候选站点之间的距离为：

$$
l_{ij}
$$

服务半径约束为：

$$
l_{ij}\leq 1000
$$

设第 $i$ 个小区由第 $j$ 个站点服务时的满意度为：

$$
s_{ij}
$$

其取值范围为：

$$
0.6\leq s_{ij}\leq 1
$$

如果 $l_{ij}>1000$，则该站点不能为小区 $i$ 提供有效服务，可设：

$$
s_{ij}=0
$$

---

### 规模参数

根据附件3，三类服务站参数如下。

| 规模 | 建设成本 | 日均固定管理成本 | 日最大服务人次 |
| ---- | -------: | ---------------: | -------------: |
| 小型 |   18万元 |        2000元/日 |    1000人次/日 |
| 中型 |   32万元 |        3200元/日 |    2000人次/日 |
| 大型 |   45万元 |        4400元/日 |    3000人次/日 |

定义建设成本为：

$$
F_k
$$

其中：

$$
F_S=18,\quad F_M=32,\quad F_L=45
$$

单位为万元。

定义日均固定管理成本为：

$$
G_k
$$

其中：

$$
G_S=2000,\quad G_M=3200,\quad G_L=4400
$$

单位为元/日。

定义日服务能力上限为：

$$
Q_k
$$

其中：

$$
Q_S=1000,\quad Q_M=2000,\quad Q_L=3000
$$

单位为人次/日。

建设预算约束为：

$$
\sum_{j\in J}\sum_{k\in K}F_k y_{j,k}\leq 120
$$

---

### 决策变量

#### 站点建设与规模选择变量

定义二元变量：

$$
y_{j,k}=
\begin{cases}
1, & \text{若在小区 }j\text{ 建设规模为 }k\text{ 的服务站}\\
0, & \text{否则}
\end{cases}
$$

每个小区最多建设一个服务站，因此有：

$$
\sum_{k\in K}y_{j,k}\leq 1,\quad \forall j\in J
$$

如果第 $j$ 个小区建设了服务站，则：

$$
y_j=\sum_{k\in K}y_{j,k}
$$

---

#### 小区分配变量

题目已说明“老人只选择满意度最高的服务站”。在选址建站完成后，每个小区应分配给可达范围内满意度最高的已建设服务站。

为了建模方便，可以定义分配变量：

$$
x_{ij}=
\begin{cases}
1, & \text{若小区 }i\text{ 分配给服务站 }j\\
0, & \text{否则}
\end{cases}
$$

若小区 $i$ 被服务站 $j$ 覆盖，则必须满足：

$$
l_{ij}\leq 1000
$$

如果 $l_{ij}>1000$，则强制：

$$
x_{ij}=0
$$

每个小区最多分配给一个服务站：

$$
\sum_{j\in J}x_{ij}\leq 1,\quad \forall i\in I
$$

只有建设了服务站的位置才能接受小区分配：

$$
x_{ij}\leq \sum_{k\in K}y_{j,k},\quad \forall i,j
$$

---

#### 覆盖变量

定义二元变量：

$$
z_i=
\begin{cases}
1, & \text{若小区 }i\text{ 至少被一个服务站有效覆盖}\\
0, & \text{否则}
\end{cases}
$$

有：

$$
z_i=\sum_{j\in J}x_{ij}
$$

由于每个小区最多分配给一个站点，所以 $z_i$ 等于0或1。

---

### 约束条件

#### 建设预算约束

总建设预算不超过120万元：

$$
\sum_{j\in J}\sum_{k\in K}F_k y_{j,k}\leq 120
$$

---

#### 单点单规模约束

每个候选小区最多建设一个服务站：

$$
\sum_{k\in K}y_{j,k}\leq 1,\quad \forall j\in J
$$

---

#### 服务半径约束

如果小区 $i$ 到站点 $j$ 的距离超过1000米，则小区 $i$ 不能由站点 $j$ 服务：

$$
x_{ij}=0,\quad \text{if }l_{ij}>1000
$$

等价地，可以定义可服务集合：

$$
A=\{(i,j)\mid l_{ij}\leq1000\}
$$

则只有当 $(i,j)\in A$ 时，$x_{ij}$ 才能取1。

---

#### 小区分配约束

每个小区最多选择一个服务站：

$$
\sum_{\substack{j\\ i\in I,\,(i,j)\in A}}x_{ij}\leq1,\quad \forall i
$$

若小区 $i$ 被覆盖，则必须分配给一个已建设服务站。

---

#### 服务能力约束

设第 $j$ 个服务站被分配的小区集合为：

$$
C_j=\{i\mid x_{ij}=1\}
$$

则该站点承担的日均服务需求为：

$$
L_j=\sum_{i\in I}d_i x_{ij}
$$

其中：

$$
d_i=\frac{\sum_{r=1}^{6}D_{i,r}^{\ast}}{30}
$$

站点服务能力约束为：

$$
\sum_{i\in I}d_i x_{ij}
\leq
\sum_{k\in K}Q_k y_{j,k},
\quad \forall j
$$

这表示分配给某个站点的日均有效需求不能超过该站点的日最大服务能力。

严格一点，也可以使用“满意度折减后的有效服务人次”进行容量约束：

$$
\sum_{i\in I}d_i s_{ij}x_{ij}
\leq
\sum_{k\in K}Q_k y_{j,k}
$$

但从服务站实际工作负担看，容量更适合约束原始服务需求 $d_i$，因为即使满意度降低，服务请求仍然存在。推荐采用：

$$
\sum_{i\in I}d_i x_{ij}
\leq
\sum_{k\in K}Q_k y_{j,k}
$$

---

#### 满意度最高选择约束

题目假设“老人只选择满意度最高的服务站”。这个约束可以在求解过程中处理，而不一定写成复杂的整数规划约束。

对于任意建站方案 $Y$，小区 $i$ 的可选站点集合为：

$$
J_i(Y)=\{j\mid y_j=1,\ l_{ij}\leq1000\}
$$

如果集合为空，则小区 $i$ 未被覆盖。

如果集合非空，则小区 $i$ 分配给满意度最高的站点：

$$
j^{\ast}(i)=\arg\max_{j\in J_i(Y)}s_{ij}
$$

然后令：

$$
x_{i,j^{\ast}(i)}=1
$$

其他：

$$
x_{ij}=0
$$

如果存在多个站点满意度相同，则选择距离更近者；若距离也相同，则选择剩余容量更大的站点。

---

### 目标函数设计

问题2要求“使服务覆盖率和服务满意度尽可能高”。因此可以采用多目标优化模型。

#### 方案一：加权综合目标函数

定义服务覆盖率为：

$$
CR=\frac{\sum_{i\in I}P_i z_i}{\sum_{i\in I}P_i}
$$

其中 $P_i$ 为第 $i$ 个小区第5年末老人总数，$z_i=1$ 表示小区 $i$ 被覆盖。

定义人口加权平均满意度为：

$$
\bar{S}
=
\frac{\sum_{i\in I}P_i z_i S_i}{\sum_{i\in I}P_i z_i}
$$

其中 $S_i$ 表示小区 $i$ 被分配服务站后的满意度：

$$
S_i=\sum_{j\in J}s_{ij}x_{ij}
$$

如果小区未被覆盖，则可设 $S_i=0$。

为了避免未覆盖小区不参与满意度平均导致结果偏高，也可以定义全体老人加权满意度：

$$
AS=
\frac{\sum_{i\in I}P_i S_i}{\sum_{i\in I}P_i}
$$

此时未覆盖小区满意度为0。

综合目标函数可写为：

$$
\max Z=\omega_1CR+\omega_2AS
$$

其中：

$$
\omega_1+\omega_2=1
$$

建议取：

$$
\omega_1=0.6,\quad \omega_2=0.4
$$

理由是养老服务站建设首先要保障服务可达与覆盖，在覆盖率达到较高水平后再提升满意度。

---

#### 方案二：分层优化目标函数

也可以采用更稳健的分层优化：

第一层目标：

$$
\max CR
$$

第二层目标：

$$
\max AS
$$

第三层目标：

$$
\max \Pi
$$

其中 $\Pi$ 表示年度利润。

也就是说，先选出覆盖率最高的方案；若多个方案覆盖率相同，再选择满意度最高的方案；若覆盖率和满意度仍相近，再选择年度利润更高或成本更低的方案。

这个方案更符合公共服务建设逻辑。因为政府建设嵌入式社区养老服务站的首要目标通常是提高覆盖与可及性，而非利润最大化。

推荐在报告中采用分层优化目标：

$$
\max CR
$$

$$
\max AS
$$

$$
\max \Pi
$$

并说明利润只作为辅助评价指标，而不是首要目标。

---

### 实际有效服务人次计算

题目给出：

$$
\text{实际有效服务人次}
=
\text{理论需求人次}\times\text{服务满意度}
$$

如果使用消费约束后的服务需求 $D_{i,r}^{\ast}$，则第 $i$ 个小区第 $r$ 项服务的实际有效月服务人次为：

$$
E_{i,r}=D_{i,r}^{\ast}S_i
$$

其中 $S_i$ 为该小区最终选择服务站后的满意度。

第 $i$ 个小区所有服务的实际有效月服务人次为：

$$
E_i=\sum_{r=1}^{6}D_{i,r}^{\ast}S_i
$$

区域总实际有效月服务人次为：

$$
E=\sum_{i=1}^{10}\sum_{r=1}^{6}D_{i,r}^{\ast}S_i
$$

折算为日均有效服务人次：

$$
E^{day}=\frac{E}{30}
$$

折算为年度有效服务人次：

$$
E^{year}=12E
$$

---

### 年度利润模型

问题2.3要求计算每个服务站预计年度利润。服务站利润需要考虑服务营收、服务直接支出、固定管理成本和建设折旧成本。

#### 年度服务收入

设第 $r$ 项服务的单次营收为：

$$
p_r
$$

第 $r$ 项服务的单次直接支出为：

$$
q_r
$$

根据附件2：

$$
p=
[10,20,30,28,25,0]
$$

$$
q=
[8,16,24,23,20,8]
$$

如果第 $i$ 个小区分配给第 $j$ 个服务站，即 $x_{ij}=1$，则第 $j$ 个服务站的年度营收为：

$$
R_j^{year}
=
12\sum_{i\in I}\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i p_r x_{ij}
$$

注意这里使用 $D_{i,r}^{\ast}S_i$，因为题目规定实际有效服务人次等于需求人次乘以满意度。

---

#### 年度直接支出

第 $j$ 个服务站的年度直接支出为：

$$
C_j^{direct}
=
12\sum_{i\in I}\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i q_r x_{ij}
$$

---

#### 年度固定管理成本

若第 $j$ 个站点选择规模 $k$，其年度固定管理成本为：

$$
C_j^{fixed}=365G_k
$$

其中：

$$
G_S=2000,\quad G_M=3200,\quad G_L=4400
$$

---

#### 年度建设折旧成本

题目说明建设投资按20年等额折旧分摊，无残值。因此若第 $j$ 个站点选择规模 $k$，年度折旧成本为：

$$
C_j^{dep}
=
\frac{10000F_k}{20}
$$

这里 $F_k$ 单位为万元，因此换算为元需要乘以10000。

即：

小型：

$$
C_S^{dep}=\frac{18\times10000}{20}=9000
$$

中型：

$$
C_M^{dep}=\frac{32\times10000}{20}=16000
$$

大型：

$$
C_L^{dep}=\frac{45\times10000}{20}=22500
$$

---

#### 服务站年度利润

第 $j$ 个服务站的预计年度利润为：

$$
\Pi_j=
R_j^{year}
\quad - C_j^{direct}
\quad - C_j^{fixed}
\quad - C_j^{dep}
$$

展开为：

$$
\Pi_j
=
12\sum_{i\in I}\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i(p_r-q_r)x_{ij}
\quad - 365G_k
\quad - \frac{10000F_k}{20}
$$

其中：

$$
p_r-q_r
$$

为第 $r$ 项服务的单次毛收益。

由附件2可得：

| 服务     | 单次营收 | 单次直接支出 | 单次毛收益 |
| -------- | -------: | -----------: | ---------: |
| 助餐     |       10 |            8 |          2 |
| 日间照料 |       20 |           16 |          4 |
| 上门护理 |       30 |           24 |          6 |
| 康复理疗 |       28 |           23 |          5 |
| 助浴     |       25 |           20 |          5 |
| 紧急救助 |        0 |            8 |         -8 |

其中紧急救助为公益免费服务，单次利润为负。因此若模型只追求利润，可能会压缩公益服务，这与养老服务站建设目标不完全一致。所以利润更适合作为评价指标，而不宜作为唯一目标。

---

### 服务覆盖率计算

服务覆盖率定义为至少享受一项养老服务的老年人数占老年总人口的比例。

如果一个小区被至少一个服务站覆盖，且分配后满意度不为0，则认为该小区老人可享受至少一项养老服务。

覆盖率为：

$$
CR
=
\frac{\sum_{i=1}^{10}P_i z_i}{\sum_{i=1}^{10}P_i}
$$

其中：

$$
z_i=
\begin{cases}
1, & \text{若小区 }i\text{ 被有效覆盖}\\
0, & \text{否则}
\end{cases}
$$

如果要更严格地按照服务能力判断覆盖率，则可定义：

$$
z_i=1
$$

当且仅当小区 $i$ 被分配给某个半径不超过1000米且有足够容量的服务站。

---

### 小区满意度计算

附件5会给出满意度评分规则，通常包含距离、服务响应和价格三个方面。因此可设：

$$
S_i=w_dS_i^{dist}+w_tS_i^{time}+w_pS_i^{price}
$$

其中：

$$
S_i^{dist}
$$

表示距离满意度；

$$
S_i^{time}
$$

表示服务响应满意度；

$$
S_i^{price}
$$

表示价格满意度。

权重满足：

$$
w_d+w_t+w_p=1
$$

题目给出满意度取值范围：

$$
0.6\leq S_i\leq 1
$$

因此计算结果可以做截断处理：

$$
S_i=\min(1,\max(0.6,S_i))
$$

但如果小区距离所有服务站都超过1000米，则认为无有效服务，满意度应设为：

$$
S_i=0
$$

因为题目明确规定超出距离不产生有效服务需求。

---

### 求解算法设计

由于只有10个候选小区，每个小区有4种状态：

不建站、小型站、中型站、大型站。

因此所有可能方案数为：

$$
4^{10}=1,048,576
$$

这个规模对于计算机来说并不大。还可以通过预算约束提前剪枝，大量方案会被过滤掉。因此可以采用完全枚举法。

---

#### 枚举法求解思路

每个小区的建站状态取值为：

$$
a_j\in\{0,S,M,L\}
$$

其中 $0$ 表示不建站。

枚举所有方案：

$$
(a_1,a_2,\dots,a_{10})
$$

对每个方案执行以下步骤：

第一步，计算建设成本：

$$
F=\sum_{\substack{j\\a_j\neq0}}F_{a_j}
$$

若：

$$
F>120
$$

则方案不可行，直接舍弃。

第二步，根据站点位置和距离矩阵，判断每个小区可达的站点集合：

$$
J_i=\{j\mid a_j\neq0,\ l_{ij}\leq1000\}
$$

若 $J_i$ 为空，则小区 $i$ 未被覆盖。

若 $J_i$ 非空，则计算小区 $i$ 对所有可达站点的满意度，并分配给满意度最高的站点：

$$
j^{\ast}(i)=\arg\max_{j\in J_i}s_{ij}
$$

第三步，计算每个服务站承担的日均需求：

$$
L_j=\sum_{\substack{i\\j^{\ast}(i)=j}}d_i
$$

若：

$$
L_j>Q_{a_j}
$$

则说明该站点超出服务能力，方案不可行。

第四步，计算覆盖率：

$$
CR=\frac{\sum_iP_iz_i}{\sum_iP_i}
$$

第五步，计算全体老人加权满意度：

$$
AS=
\frac{\sum_iP_iS_i}{\sum_iP_i}
$$

第六步，计算各服务站年度利润：

$$
\Pi_j=
12\sum_{\substack{i\\j^{\ast}(i)=j}}\sum_rD_{i,r}^{\ast}S_i(p_r-q_r)
\quad - 365G_{a_j}
\quad - \frac{10000F_{a_j}}{20}
$$

第七步，根据目标函数选择最优方案。

若采用加权目标：

$$
Z=0.6CR+0.4AS
$$

则选择 $Z$ 最大的方案。

若采用分层目标，则按以下优先级排序：

$$
CR\rightarrow AS\rightarrow \sum_j\Pi_j
$$

即先比较覆盖率，再比较满意度，最后比较年度总利润。

---

#### 伪代码

```text
输入：
    第5年末各小区老人数量 P_i
    消费约束后的服务需求 D_i,r*
    距离矩阵 L_i,j
    满意度规则
    站点成本、容量、固定成本参数

初始化：
    best_solution = None
    best_score = -∞

枚举所有建站方案 a = (a_1, ..., a_10)，其中 a_j ∈ {0, 小型, 中型, 大型}：

    计算总建设成本 total_build_cost

    如果 total_build_cost > 120万元：
        跳过该方案

    对每个小区 i：
        找出所有可达站点 J_i = {j | a_j ≠ 0 且 L_i,j ≤ 1000}

        如果 J_i 为空：
            z_i = 0
            S_i = 0
            continue

        计算小区 i 对每个可达站点 j 的满意度 S_i,j

        选择满意度最高的站点 j*
        x_i,j* = 1
        z_i = 1
        S_i = S_i,j*

    对每个服务站 j：
        计算其承担的日均需求 L_j

        如果 L_j > 该站点容量 Q_j：
            标记为不可行方案
            跳过该方案

    计算覆盖率 CR

    计算全体老人加权满意度 AS

    计算每个服务站年度利润 Pi_j 和总利润 Pi_total

    计算综合目标值 score
        score = 0.6 * CR + 0.4 * AS
        或者使用分层排序指标 (CR, AS, Pi_total)

    如果当前方案优于 best_solution：
        更新 best_solution

输出：
    最优站点数量、位置、规模
    每个站点覆盖的小区
    每个站点预计年度利润
    服务覆盖率
    每个小区老人满意度
```

---

### 时间复杂度分析

共有10个候选位置，每个位置有4种选择：

$$
0,\ S,\ M,\ L
$$

因此枚举方案数为：

$$
4^{10}
$$

对每个方案，需要对10个小区和最多10个站点计算可达性与满意度，复杂度为：

$$
O(n^2)
$$

其中：

$$
n=10
$$

因此总时间复杂度为：

$$
O(4^n n^2)
$$

当 $n=10$ 时：

$$
4^{10}\times10^2
=
1048576\times100
$$

计算量在普通计算机上可以接受。

如果后续小区数量扩大到几十个甚至上百个，则完全枚举法会变得不可行，需要改用整数规划、遗传算法、模拟退火、粒子群算法或贪心启发式算法。

---

### 也可以使用整数规划模型

如果希望报告更“优化模型化”，可以把问题写成混合整数规划模型。

#### 目标函数

$$
\max Z
=
\omega_1
\frac{\sum_iP_iz_i}{\sum_iP_i}
+ \omega_2
\frac{\sum_iP_i\sum_js_{ij}x_{ij}}{\sum_iP_i}
$$

其中：

$$
\omega_1+\omega_2=1
$$

#### 约束条件

预算约束：

$$
\sum_j\sum_kF_ky_{j,k}\leq120
$$

单点单规模约束：

$$
\sum_ky_{j,k}\leq1,\quad \forall j
$$

分配约束：

$$
\sum_jx_{ij}\leq1,\quad \forall i
$$

服务半径约束：

$$
x_{ij}=0,\quad l_{ij}>1000
$$

分配必须依赖建站：

$$
x_{ij}\leq\sum_ky_{j,k},\quad \forall i,j
$$

容量约束：

$$
\sum_id_ix_{ij}
\leq
\sum_kQ_ky_{j,k},\quad \forall j
$$

覆盖变量约束：

$$
z_i=\sum_jx_{ij}
$$

变量取值约束：

$$
x_{ij},y_{j,k},z_i\in\{0,1\}
$$

这个模型形式更规范，但“老人只选择满意度最高服务站”这一规则在线性规划中比较难直接表达。实际求解时，可以先枚举建站方案，再按满意度最高原则进行小区分配。因此对于本题，我更建议使用“枚举建站方案 + 规则分配 + 目标评价”的方法。

---

### 后续结果分析应输出的内容

后续集中求解时，问题2.3建议输出以下几张表。

#### 表1：最优站点建设方案

| 站点编号 | 建设位置 | 规模           | 建设成本/万元 | 日服务能力 | 日固定成本 |
| -------- | -------- | -------------- | ------------: | ---------: | ---------: |
| 1        | 小区X    | 小型/中型/大型 |               |            |            |
| 2        | 小区Y    | 小型/中型/大型 |               |            |            |

---

#### 表2：服务站覆盖小区

| 服务站位置 | 覆盖小区 | 覆盖老人数量 | 日均需求人次 | 容量利用率 |
| ---------- | -------- | -----------: | -----------: | ---------: |
| 小区X      | A、B、C  |              |              |            |
| 小区Y      | D、E     |              |              |            |

容量利用率为：

$$
U_j=\frac{L_j}{Q_j}
$$

---

#### 表3：每个小区满意度

| 小区 | 分配服务站 | 距离 | 满意度 | 是否有效覆盖 |
| ---- | ---------- | ---: | -----: | ------------ |
| A    | 小区X      |      |        | 是           |
| B    | 小区X      |      |        | 是           |
| C    | 小区Y      |      |        | 是           |

---

#### 表4：每个服务站年度利润

| 服务站 | 年度营收 | 年度直接支出 | 年度固定管理成本 | 年度折旧成本 | 年度利润 |
| ------ | -------: | -----------: | ---------------: | -----------: | -------: |
| 小区X  |          |              |                  |              |          |
| 小区Y  |          |              |                  |              |          |

---

#### 表5：总体评价指标

| 指标               | 数值 |
| ------------------ | ---: |
| 建设站点数量       |      |
| 总建设成本         |      |
| 服务覆盖率         |      |
| 全体老人加权满意度 |      |
| 区域年度总利润     |      |
| 日均总服务需求     |      |
| 日均有效服务人次   |      |

---

### 模型局限性

后续报告中至少可以写以下三点。

#### 人群需求被平均化，忽略个体差异

模型使用每类老人月均服务需求次数表示整体需求，默认同一类型老人具有相同服务频率。但现实中，老人年龄、慢性病情况、家庭照护条件、收入水平、居住楼层、子女陪伴情况等都会影响实际需求。因此，模型可能高估部分老人需求，也可能低估高风险老人需求。

#### 服务站容量被简化为总人次容量

模型将服务站能力统一表示为“日最大服务人次”，没有区分不同服务项目对人员、设备和时间的占用差异。例如，上门护理和紧急救助需要专业人员与移动时间，助餐则更依赖餐食供应能力，康复理疗可能受到设备数量限制。统一容量处理会弱化不同服务类型之间的资源差异。

#### 满意度模型存在静态化问题

满意度主要由距离、响应和价格决定，且在模型中被视为固定值。但现实中的满意度会受到服务质量、人员态度、等待时间、预约便利性、站点环境和长期信任关系影响。随着站点负荷变化，响应速度也会变化，因此满意度应当与容量利用率、排队时间等因素动态关联。

#### 服务分配规则较理想化

模型假设老人只选择满意度最高的服务站，并且每个小区整体分配给同一个站点。但现实中，同一小区老人可能因服务类型、出行能力、家庭位置和个人偏好选择不同站点。将整个小区统一分配给单个站点会降低模型精度。

#### 成本和收入参数被视为长期稳定

模型假设服务价格、工资水平、物价和补贴政策在规划期内稳定。但养老服务高度依赖人工成本和政策补贴，未来五年可能出现较大变化。因此，年度利润测算只能作为静态估计结果。

---

### 改进方向

可以提出以下改进方向。

#### 引入多服务类型容量约束

将“总人次容量”拆分为不同服务项目的专项容量。例如：

$$
Q_{j,r}
$$

表示服务站 $j$ 对服务 $r$ 的日服务能力。这样可以分别约束助餐、日间照料、上门护理、康复理疗、助浴和紧急救助的容量，更符合真实运营情况。

#### 构建动态满意度模型

将满意度设置为距离、价格和容量利用率的函数：

$$
S_{ij}=f(l_{ij},price_i,U_j)
$$

其中：

$$
U_j=\frac{L_j}{Q_j}
$$

表示服务站容量利用率。当站点负荷过高时，响应时间变长，满意度下降。这样可以避免某些站点虽然距离近，但因过载导致服务体验下降的问题。

#### 引入多目标优化算法

如果后续小区数量扩大，可以采用 NSGA-II、遗传算法、模拟退火或粒子群算法求解，在覆盖率、满意度、建设成本和年度利润之间形成 Pareto 最优解集。这样可以为政府提供多个可选方案，而不是单一最优方案。

#### 结合GIS空间数据优化选址

当前模型只允许服务站建在10个小区内部。如果获得更精细的道路网络、步行时间、公交可达性和实际建筑空间数据，可以把候选点扩展为若干真实地块，并用路网距离替代直线距离，从而得到更可实施的选址方案。

---

### 推荐最终求解策略

本题后续集中求解时，建议采用以下主方案：

采用“枚举建站方案 + 满意度最高分配 + 容量校验 + 分层目标筛选”的方法。

具体目标优先级为：

$$
\max \text{服务覆盖率}
$$

在覆盖率相同或接近时：

$$
\max \text{全体老人加权满意度}
$$

若仍存在多个候选方案：

$$
\max \text{区域年度总利润}
$$

这种求解策略符合养老服务站的公共服务属性，也便于解释结果。它既能保证站点布局尽可能覆盖更多老人，又能考虑服务体验和财政可持续性。

---

# 问题3：服务定价与政府补贴优化

为降低老人负担，政府决定对服务站给予补贴，以降低服务价格。

## 3.0 相关情况说明

### 政府补贴政策

- 除紧急救助外，其他服务按实际有效服务人次补贴：**2元/人次**
- 单个服务站每日补贴最高限额：
  - 小型：**1000元**
  - 中型：**1800元**
  - 大型：**2600元**
- 紧急救助公益免费，不计补贴，仅核算直接支出

### 利润率要求

- 养老机构需保本微利，利润率 ≤ **8%**

### 利润率计算公式

$$
\text{利润率} = \frac{\text{服务总利润} + \text{政府补贴} - \text{年运营成本总额}}{\text{年运营成本总额}} \times 100\%
$$

## 3.1 最优定价模型建立

**假设**：服务站的选址和规模已按问题2的最优方案确定

**目标**：

- 允许每个服务站自主定价
- 以"最大化老人满意度"为目标
- 在考虑政府补贴及利润率要求情况下
- 确定各项服务的最优定价（元/次）

## 3.2 模型求解与结果分析

**任务**：

- 求解上述模型，给出最优定价
- 计算每个服务站预计年度利润及利润率
- 计算每个小区老人满意度得分
- 单独列出其中的"价格满意度得分"

## 3.3 服务可及性分析

**任务**：分析定价与补贴对不同类型老人（自理、半失能、失能）服务可及性的影响。

**可及性定义**：老年人能否在经济上、地理上、信息等维度方便地获得所需服务

---

问题3可以建成一个“已知站点布局后的服务定价—补贴—利润率约束优化模型”。它和问题2的关系是：问题2先确定服务站的位置、规模、覆盖小区和基础满意度结构；问题3在这个固定布局上，进一步决定各服务站各类服务的收费价格，使老人满意度尽可能高，同时保证服务站保本微利、利润率不超过 8%。

这里先给出建模思路，不集中做数值求解。

---

### 问题3的核心逻辑

问题3的决策对象是服务价格。由于服务站的位置和规模已经由问题2确定，所以问题3不再改变：

$$
\text{站点位置}
$$

$$
\text{站点规模}
$$

$$
\text{服务半径}
$$

$$
\text{服务站覆盖小区}
$$

问题3只改变：

$$
\text{各服务站各项服务的单次收费价格}
$$

服务价格变化会影响老人价格满意度 $S_3$，进而影响总满意度：

$$
S=0.2S_1+0.3S_2+0.5S_3
$$

同时，价格变化会影响服务站收入；政府补贴又会增加服务站收入来源；利润率上限约束保证养老机构不能过度盈利。

因此，问题3是一个带补贴约束和利润率约束的定价优化问题。

---

### 符号定义

设问题2已经确定服务站集合为：

$$
J^{\ast}
$$

其中 $j\in J^{\ast}$ 表示已建设服务站。

设服务类型集合为：

$$
R=\{1,2,3,4,5,6\}
$$

分别表示：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

设第 $j$ 个服务站的规模为：

$$
k_j\in \{S,M,L\}
$$

其中：

$$
S=\text{小型},\quad M=\text{中型},\quad L=\text{大型}
$$

设第 $i$ 个小区由第 $j$ 个服务站服务，分配变量为：

$$
x_{ij}=
\begin{cases}
1, & \text{若小区 }i\text{ 由服务站 }j\text{ 服务}\\
0, & \text{否则}
\end{cases}
$$

该变量由问题2的最优选址结果确定，在问题3中视为已知参数。

设第 $i$ 个小区第 $r$ 类服务在消费约束后的月需求人次为：

$$
D_{i,r}^{\ast}
$$

设第 $i$ 个小区第 $r$ 类服务的年需求人次为：

$$
A_{i,r}=12D_{i,r}^{\ast}
$$

---

### 定价变量

允许每个服务站自主定价，因此定义决策变量：

$$
p_{j,r}
$$

表示第 $j$ 个服务站第 $r$ 项服务的单次收费价格。

由于紧急救助公益免费，题目明确说明不计补贴，仅核算直接支出，因此紧急救助价格固定为：

$$
p_{j,6}=0
$$

对其他五类收费服务：

$$
p_{j,r}\geq0,\quad r=1,2,3,4,5
$$

附件2给出的基准价格为：

$$
p_r^0=[10,20,30,28,25,0]
$$

其中服务顺序为：

$$
\text{助餐、日间照料、上门护理、康复理疗、助浴、紧急救助}
$$

单次直接支出为：

$$
c_r=[8,16,24,23,20,8]
$$

---

### 价格满意度模型

附件5规定价格满意度 $S_3$ 按价格相对基准价的溢价幅度确定。

对第 $j$ 个服务站第 $r$ 项服务，价格满意度可定义为：

$$
S_{3,j,r}=
\begin{cases}
1.00, & p_{j,r}\leq p_r^0\\
0.90, & p_r^0<p_{j,r}\leq1.1p_r^0\\
0.75, & 1.1p_r^0<p_{j,r}\leq1.2p_r^0\\
0.60, & p_{j,r}>1.2p_r^0
\end{cases}
$$

对紧急救助，由于公益免费：

$$
S_{3,j,6}=1
$$

由于一个小区会使用多个服务项目，因此该小区面对服务站 $j$ 的综合价格满意度可以按该小区各项服务需求占比加权计算：

$$
S_{3,i,j}
=
\frac{
\sum_{r=1}^{6}D_{i,r}^{\ast}S_{3,j,r}
}{
\sum_{r=1}^{6}D_{i,r}^{\ast}
}
$$

如果希望更贴近支付负担，也可以按服务消费金额加权：

$$
S_{3,i,j}
=
\frac{
\sum_{r=1}^{6}D_{i,r}^{\ast}p_r^0S_{3,j,r}
}{
\sum_{r=1}^{6}D_{i,r}^{\ast}p_r^0
}
$$

但考虑到紧急救助基准价为0，且题目要求的是服务满意度，建议采用“服务人次加权”的方式，更稳定，也更容易解释。

---

### 距离满意度模型

附件5规定距离满意度 $S_1$ 为：

$$
S_{1,ij}=
\begin{cases}
1.00, & l_{ij}\leq300\\
0.90, & 300<l_{ij}\leq500\\
0.75, & 500<l_{ij}\leq650\\
0.60, & 650<l_{ij}\leq1000
\end{cases}
$$

如果：

$$
l_{ij}>1000
$$

则该小区不能由该服务站有效服务，问题2中已经排除这种分配。

---

### 响应满意度模型

附件5规定响应满意度 $S_2$ 由服务站利用率决定。

服务站利用率定义为：

$$
u_j=\frac{\text{服务站实际服务人次}}{\text{服务站最大服务人次}}
$$

由于服务站能力是按日计算的，所以应使用日均实际服务人次。

设第 $j$ 个服务站的日最大服务人次为：

$$
Q_j
$$

其中：

$$
Q_S=1000,\quad Q_M=2000,\quad Q_L=3000
$$

第 $j$ 个服务站的日均实际有效服务人次为：

$$
E_j^{day}
=
\frac{1}{30}
\sum_{i=1}^{10}
\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i x_{ij}
$$

于是：

$$
u_j=\frac{E_j^{day}}{Q_j}
$$

响应满意度为：

$$
S_{2,j}=
\begin{cases}
1.00, & u_j\leq0.60\\
0.93, & 0.60<u_j\leq0.75\\
0.85, & 0.75<u_j\leq0.85\\
0.72, & 0.85<u_j\leq0.95\\
0.60, & 0.95<u_j\leq1.00
\end{cases}
$$

这里存在一个小的循环关系：

$$
S_i \rightarrow E_j^{day}\rightarrow u_j\rightarrow S_{2,j}\rightarrow S_i
$$

因为实际有效服务人次由满意度决定，而响应满意度又由实际有效服务人次决定。

实际求解时可以采用两种处理方式。

第一种是简化处理。用问题2得到的站点原始日需求量计算利用率：

$$
u_j=
\frac{
\frac{1}{30}\sum_i\sum_rD_{i,r}^{\ast}x_{ij}
}{
Q_j
}
$$

这种做法不再把满意度折减纳入利用率，计算更稳定。服务站真实工作负荷通常也更接近原始需求，而不是满意度折减后的需求。因此这是推荐做法。

第二种是固定点迭代。先给定初始 $S_{2,j}$，计算满意度和实际服务人次，再更新利用率和响应满意度，直到结果不再变化。这种方法更贴合题面“实际服务人次”的表述，但计算略复杂。

在报告中建议采用第一种简化处理，理由是服务站的响应压力主要来源于潜在服务请求量，而不是满意度折减后的有效服务量。

---

### 总满意度模型

第 $i$ 个小区由第 $j$ 个服务站服务时，总满意度为：

$$
S_{ij}
=
0.2S_{1,ij}+0.3S_{2,j}+0.5S_{3,i,j}
$$

若第 $i$ 个小区由问题2分配给服务站 $j$，则该小区满意度为：

$$
S_i=\sum_{j\in J^{\ast}}x_{ij}S_{ij}
$$

由于 $x_{ij}$ 已知且每个小区最多分配给一个服务站，上式等价于该小区所分配站点对应的满意度。

全区域老人加权平均满意度为：

$$
\bar{S}
=
\frac{
\sum_{i=1}^{10}P_iS_i
}{
\sum_{i=1}^{10}P_i
}
$$

其中 $P_i$ 为第5年末第 $i$ 个小区老人总数。

---

### 政府补贴模型

政府补贴规则为：

除紧急救助外，其他服务按实际有效服务人次补贴：

$$
2\text{元/人次}
$$

紧急救助不计补贴。

第 $j$ 个服务站年度可补贴服务人次为：

$$
E_{j}^{sub}
=
12
\sum_{i=1}^{10}
\sum_{r=1}^{5}
D_{i,r}^{\ast}S_i x_{ij}
$$

注意这里服务类型只统计 $r=1,\dots,5$，不包括紧急救助。

若没有日补贴上限，则年度补贴额为：

$$
2E_j^{sub}
$$

但题目给出了单个服务站每日补贴上限。设第 $j$ 个服务站对应规模的每日补贴上限为：

$$
H_j
$$

其中：

$$
H_S=1000,\quad H_M=1800,\quad H_L=2600
$$

则第 $j$ 个服务站日均理论补贴为：

$$
B_j^{day,0}
=
2
\cdot
\frac{1}{30}
\sum_{i=1}^{10}
\sum_{r=1}^{5}
D_{i,r}^{\ast}S_i x_{ij}
$$

考虑每日补贴上限后，日均实际补贴为：

$$
B_j^{day}
=
\min(B_j^{day,0},H_j)
$$

年度政府补贴为：

$$
B_j^{year}
=
365B_j^{day}
$$

也可以按月折算为：

$$
B_j^{year}
=
12\min
\left(
2\sum_i\sum_{r=1}^{5}D_{i,r}^{\ast}S_i x_{ij},
30H_j
\right)
$$

这两种写法含义一致。考虑题目中需求是月需求，建议使用第二种按月转年度的表达：

$$
B_j^{year}
=
12\min
\left(
2\sum_i\sum_{r=1}^{5}D_{i,r}^{\ast}S_i x_{ij},
30H_j
\right)
$$

---

### 服务收入与成本模型

#### 年度服务收入

第 $j$ 个服务站年度服务收入为：

$$
R_j^{year}
=
12
\sum_{i=1}^{10}
\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i p_{j,r}x_{ij}
$$

由于紧急救助价格为0，所以第6类服务不产生服务收入。

---

#### 年度直接支出

第 $j$ 个服务站年度直接支出为：

$$
C_j^{direct}
=
12
\sum_{i=1}^{10}
\sum_{r=1}^{6}
D_{i,r}^{\ast}S_i c_rx_{ij}
$$

其中：

$$
c=[8,16,24,23,20,8]
$$

紧急救助虽然免费且不补贴，但仍有直接支出：

$$
c_6=8
$$

---

#### 年度固定运营成本

若第 $j$ 个服务站规模为 $k_j$，其日均固定管理成本为 $G_{k_j}$，则年度固定管理成本为：

$$
C_j^{fixed}=365G_{k_j}
$$

其中：

$$
G_S=2000,\quad G_M=3200,\quad G_L=4400
$$

---

#### 年度折旧成本

建设投资按20年等额折旧、无残值。若第 $j$ 个站点规模为 $k_j$，建设成本为 $F_{k_j}$ 万元，则年度折旧成本为：

$$
C_j^{dep}
=
\frac{10000F_{k_j}}{20}
$$

对应为：

| 规模 | 建设成本 | 年折旧成本 |
|---|---:|---:|
| 小型 | 18万元 | 9000元/年 |
| 中型 | 32万元 | 16000元/年 |
| 大型 | 45万元 | 22500元/年 |

---

#### 年运营成本总额

题目中的“年运营成本总额”建议定义为：

$$
C_j^{year}
=
C_j^{direct}+C_j^{fixed}+C_j^{dep}
$$

即包括服务直接支出、固定管理成本和建设折旧成本。

如果希望严格区分“运营成本”和“建设折旧”，也可以将折旧单独列示。但题目已经说明建设投资按20年等额折旧分摊，年均分摊成本计入年度总成本，因此应将折旧纳入年度总成本。

---

### 利润与利润率模型

第 $j$ 个服务站年度净利润为：

$$
\Pi_j
=
R_j^{year}
+
B_j^{year}
-
C_j^{year}
$$

其中：

$$
R_j^{year}
$$

为服务收入；

$$
B_j^{year}
$$

为政府补贴；

$$
C_j^{year}
$$

为年度总成本。

题目给出的利润率公式为：

$$
\text{利润率}
=
\frac{
\text{服务总利润}+\text{政府补贴}-\text{年运营成本总额}
}{
\text{年运营成本总额}
}
\times100\%
$$

为了避免“服务总利润”与“服务收入”概念混淆，建议在模型中明确采用：

$$
\rho_j
=
\frac{
R_j^{year}+B_j^{year}-C_j^{year}
}{
C_j^{year}
}
$$

利润率要求为：

$$
0\leq \rho_j\leq 8\%
$$

这里下界 $0$ 表示保本要求；上界 $8\%$ 表示微利上限。

如果不加下界，模型为了最大化满意度会倾向于把价格压到很低，甚至导致服务站长期亏损，这不符合“保本微利”的要求。因此应加入：

$$
R_j^{year}+B_j^{year}\geq C_j^{year}
$$

即：

$$
\rho_j\geq0
$$

同时加入：

$$
R_j^{year}+B_j^{year}\leq1.08C_j^{year}
$$

即：

$$
\rho_j\leq0.08
$$

---

### 优化目标函数

问题3要求以“最大化老人满意度”为目标。

因此目标函数可设为：

$$
\max \bar{S}
=
\max
\frac{
\sum_{i=1}^{10}P_iS_i
}{
\sum_{i=1}^{10}P_i
}
$$

其中：

$$
S_i=0.2S_{1,ij}+0.3S_{2,j}+0.5S_{3,i,j}
$$

且 $j$ 为小区 $i$ 在问题2中分配到的服务站。

该目标会推动服务价格尽可能不高于基准价，因为价格越低，价格满意度越高：

$$
p_{j,r}\leq p_r^0
\Rightarrow
S_{3,j,r}=1
$$

但价格过低会导致服务站无法保本，因此利润率约束会迫使价格保持在一个可持续水平。

---

### 完整优化模型

综上，问题3可以写成：

$$
\max \bar{S}
=
\frac{
\sum_iP_iS_i
}{
\sum_iP_i
}
$$

决策变量：

$$
p_{j,r},\quad j\in J^{\ast},\ r=1,\dots,5
$$

固定条件：

$$
p_{j,6}=0
$$

满意度约束：

$$
S_i
=
\sum_{j\in J^{\ast}}x_{ij}
\left(
0.2S_{1,ij}+0.3S_{2,j}+0.5S_{3,i,j}
\right)
$$

价格满意度由分段函数决定：

$$
S_{3,j,r}=
\begin{cases}
1.00, & p_{j,r}\leq p_r^0\\
0.90, & p_r^0<p_{j,r}\leq1.1p_r^0\\
0.75, & 1.1p_r^0<p_{j,r}\leq1.2p_r^0\\
0.60, & p_{j,r}>1.2p_r^0
\end{cases}
$$

年度收入：

$$
R_j^{year}
=
12
\sum_i\sum_rD_{i,r}^{\ast}S_ip_{j,r}x_{ij}
$$

年度补贴：

$$
B_j^{year}
=
12\min
\left(
2\sum_i\sum_{r=1}^{5}D_{i,r}^{\ast}S_ix_{ij},
30H_j
\right)
$$

年度成本：

$$
C_j^{year}
=
12\sum_i\sum_rD_{i,r}^{\ast}S_ic_rx_{ij}
+
365G_{k_j}
+
\frac{10000F_{k_j}}{20}
$$

利润率约束：

$$
0
\leq
\frac{R_j^{year}+B_j^{year}-C_j^{year}}{C_j^{year}}
\leq
0.08
$$

非负价格约束：

$$
p_{j,r}\geq0,\quad r=1,\dots,5
$$

紧急救助公益约束：

$$
p_{j,6}=0
$$

---

### 模型求解思路

由于价格满意度是分段函数，而且满意度只在几个价格区间发生变化，因此没有必要对价格做连续搜索。可以把每项服务价格离散化到几个关键价格点。

对每项收费服务 $r=1,\dots,5$，设置候选价格集合：

$$
\mathcal{P}_r=
\{p_r^0,\ 1.1p_r^0,\ 1.2p_r^0,\ p_r^{max}\}
$$

其中：

$$
p_r^{max}
$$

可以设置为 $1.3p_r^0$ 或 $1.5p_r^0$。

但为了最大化满意度，若某个价格区间内满意度相同，则应选择该区间内价格最高的点来提高收入、帮助保本。例如：

平价区间：

$$
p_{j,r}\leq p_r^0
$$

价格满意度均为1。若为了在不降低满意度的情况下增加收入，应选择：

$$
p_{j,r}=p_r^0
$$

微溢价区间：

$$
p_r^0<p_{j,r}\leq1.1p_r^0
$$

价格满意度均为0.90。若进入该区间，应选择：

$$
p_{j,r}=1.1p_r^0
$$

中溢价区间：

$$
1.1p_r^0<p_{j,r}\leq1.2p_r^0
$$

价格满意度均为0.75。若进入该区间，应选择：

$$
p_{j,r}=1.2p_r^0
$$

高溢价区间价格满意度均为0.60，不利于目标函数，一般只在保本需要时考虑。可设置一个上界，例如：

$$
p_{j,r}\leq1.3p_r^0
$$

因此每项服务只需枚举有限个价格点。

---

#### 推荐候选价格集合

基准价为：

| 服务 | 基准价 |
|---|---:|
| 助餐 | 10 |
| 日间照料 | 20 |
| 上门护理 | 30 |
| 康复理疗 | 28 |
| 助浴 | 25 |
| 紧急救助 | 0 |

收费服务候选价格可以设为：

| 服务 | 平价 | 微溢价 | 中溢价 | 高溢价上界 |
|---|---:|---:|---:|---:|
| 助餐 | 10 | 11 | 12 | 13 |
| 日间照料 | 20 | 22 | 24 | 26 |
| 上门护理 | 30 | 33 | 36 | 39 |
| 康复理疗 | 28 | 30.8 | 33.6 | 36.4 |
| 助浴 | 25 | 27.5 | 30 | 32.5 |

紧急救助固定为：

$$
p_{j,6}=0
$$

---

### 算法设计

在问题2站点方案固定后，问题3可以采用枚举搜索。

#### 单站点价格枚举

对每个服务站 $j$，枚举五类收费服务的候选价格组合：

$$
(p_{j,1},p_{j,2},p_{j,3},p_{j,4},p_{j,5})
$$

如果每项服务有4个候选价格，则单个服务站共有：

$$
4^5=1024
$$

种价格组合。

对每个组合，计算：

1. 价格满意度；
2. 小区总满意度；
3. 年度服务收入；
4. 年度直接支出；
5. 政府补贴；
6. 年度利润；
7. 利润率；
8. 是否满足 $0\leq \rho_j\leq8\%$。

在所有可行组合中，选择使该服务站覆盖小区老人加权满意度最高的价格组合。

---

#### 多站点联合枚举

如果问题2建成了多个服务站，由于每个小区已经固定分配到某个站点，且每个服务站的定价、补贴、利润率都可以按站点独立计算，因此问题3可以按服务站分别求解。

这比全局枚举更高效。

若有 $m$ 个服务站，则逐站求解复杂度为：

$$
O(m\cdot4^5)
$$

而不是：

$$
O((4^5)^m)
$$

因此计算量很小。

---

#### 求解伪代码

```text
输入：
    问题2最优站点位置、规模、覆盖小区
    各小区消费约束后的服务需求 D_i,r*
    各小区老人数量 P_i
    距离矩阵
    满意度规则
    服务基准价格、直接支出
    政府补贴规则

对每个服务站 j：

    构造五类收费服务的候选价格集合

    best_price_j = None
    best_satisfaction_j = -∞

    枚举所有价格组合 price_combo：

        计算各服务的价格满意度 S3_j,r

        对服务站 j 覆盖的每个小区 i：
            计算综合价格满意度 S3_i,j
            计算距离满意度 S1_i,j
            计算响应满意度 S2_j
            计算总满意度 S_i

        计算服务站年度收入 R_j

        计算服务站年度直接支出 C_direct_j

        计算年度固定成本 C_fixed_j

        计算年度折旧成本 C_dep_j

        计算政府补贴 B_j

        计算年度总成本 C_year_j

        计算利润率 rho_j

        如果 0 <= rho_j <= 0.08：
            计算服务站覆盖区域老人加权满意度

            如果满意度更高：
                更新 best_price_j

输出：
    每个服务站最优定价
    每个服务站年度利润和利润率
    每个小区满意度
    每个小区价格满意度
```

---

### 时间复杂度分析

设服务站数量为 $m$，收费服务种类数为 $r=5$，每项服务候选价格数量为 $h=4$，小区数量为 $n=10$。

单个服务站价格组合数为：

$$
h^r=4^5=1024
$$

每个价格组合需要计算该站点覆盖小区的满意度、收入和成本，最坏情况下覆盖所有小区，复杂度为：

$$
O(nr)
$$

因此总时间复杂度为：

$$
O(mh^rnr)
$$

由于 $m\leq10$、$n=10$、$r=5$、$h=4$，计算量非常小，适合直接枚举。

---

### 问题3结果输出表设计

后续集中求解时，问题3建议输出以下表格。

#### 表1：各服务站最优定价表

| 服务站 | 规模 | 助餐 | 日间照料 | 上门护理 | 康复理疗 | 助浴 | 紧急救助 |
|---|---|---:|---:|---:|---:|---:|---:|
| 站点1 | 小/中/大 |  |  |  |  |  | 0 |
| 站点2 | 小/中/大 |  |  |  |  |  | 0 |

---

#### 表2：各服务站年度利润与利润率

| 服务站 | 年度收入 | 政府补贴 | 年度直接支出 | 固定管理成本 | 折旧成本 | 年度利润 | 利润率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 站点1 |  |  |  |  |  |  |  |
| 站点2 |  |  |  |  |  |  |  |

---

#### 表3：各小区满意度得分

| 小区 | 分配服务站 | 距离满意度S1 | 响应满意度S2 | 价格满意度S3 | 综合满意度S |
|---|---|---:|---:|---:|---:|
| A |  |  |  |  |  |
| B |  |  |  |  |  |

---

#### 表4：不同类型老人服务可及性分析

| 老人类型 | 经济可及性 | 地理可及性 | 服务可及性 | 主要影响因素 |
|---|---|---|---|---|
| 自理老人 | 高 | 较高 | 高 | 消费上限较宽松，主要受距离影响 |
| 半失能老人 | 中 | 中 | 中 | 服务频次较高，价格和距离均有影响 |
| 失能老人 | 较低 | 较低 | 较低 | 服务需求高，对上门护理、助浴、康复服务依赖强 |

---

### 服务可及性分析思路

问题3.3要求分析定价与补贴对不同类型老人服务可及性的影响。可以从三个维度展开。

#### 经济可及性

经济可及性主要由服务价格、老人收入和消费上限决定。

自理老人理论服务费用较低，前面计算过：

$$
C_1^0=356\text{元/月}
$$

其消费上限为：

$$
0.2I_i
$$

即使在收入最低的小区 F：

$$
0.2\times2700=540
$$

仍高于356元。因此，自理老人基本不受消费约束影响，经济可及性较高。

半失能老人理论服务费用为：

$$
C_2^0=822\text{元/月}
$$

其消费上限为：

$$
0.25I_i
$$

低收入小区可能不足以完全覆盖理论服务需求。因此，半失能老人对补贴和定价更敏感。

失能老人理论服务费用为：

$$
C_3^0=1208\text{元/月}
$$

其消费上限为：

$$
0.3I_i
$$

即使在高收入小区，消费上限也可能接近或低于理论费用。因此，失能老人经济可及性最弱，对政府补贴最依赖。

---

#### 地理可及性

地理可及性主要由服务站距离决定。根据附件5，距离越近，距离满意度越高。

$$
l_{ij}\leq300m \Rightarrow S_1=1.00
$$

$$
650m<l_{ij}\leq1000m \Rightarrow S_1=0.60
$$

超过1000米时，不产生有效服务需求。

对于自理老人，较远距离仍可能接受，因为其行动能力较强。

对于半失能老人，距离会显著影响日间照料、康复理疗等需要到站服务的可及性。

对于失能老人，地理可及性更依赖上门护理、助浴、紧急救助等上门类服务。即使服务站在1000米范围内，如果距离较远，响应满意度和实际服务体验仍可能下降。

---

### 信息可及性

信息可及性指老人是否能够方便地了解服务内容、价格、补贴政策和预约方式。

价格补贴政策如果不透明，老人可能仍然按照原价格预期判断服务负担，从而降低服务使用意愿。因此，定价和补贴政策不仅要降低实际价格，也要通过公示、社区通知、网格员宣传、家属端小程序等方式提升信息可达性。

对于自理老人，线上预约和社区公告较容易发挥作用。

对于半失能和失能老人，应更多依赖家属、社区工作人员、上门评估和电话预约等辅助渠道。

---

### 补贴对不同类型老人的影响

政府补贴对三类老人的影响程度不同。

#### 自理老人

自理老人主要需求集中在助餐和日间照料，服务价格相对较低，原本消费压力不大。补贴可以进一步降低其服务成本，但边际改善有限。对自理老人来说，地理距离和服务便利性可能比价格更重要。

#### 半失能老人

半失能老人开始产生上门护理、康复理疗、助浴等较高成本服务需求。补贴可以明显缓解其费用压力，提高其对专业照护服务的使用率。若补贴使服务站能够维持平价或低溢价，则半失能老人价格满意度会明显提高。

#### 失能老人

失能老人服务频次最高，服务结构中上门护理、康复理疗、助浴和紧急救助占比更高。由于紧急救助免费但有直接支出，服务站可能面临公益服务成本压力。政府补贴虽然不覆盖紧急救助，但可通过补贴其他服务提高服务站整体收支平衡能力，间接保障失能老人服务可及性。

总体来看，补贴对失能老人和半失能老人的改善作用更强，对自理老人主要起到锦上添花作用。

---

### 推荐写入报告的表述

可以这样写：

在服务站选址和规模确定后，本文进一步构建服务定价与政府补贴优化模型。模型以各服务站各项服务单次收费价格为决策变量，以老人加权平均满意度最大化为目标，在政府补贴规则和养老机构保本微利约束下确定最优定价方案。满意度由距离满意度、响应满意度和价格满意度共同构成，其中距离满意度由小区至服务站距离决定，响应满意度由服务站容量利用率决定，价格满意度由服务价格相对基准价的溢价程度决定。

考虑到政府对除紧急救助外的服务按实际有效服务人次提供每人次2元补贴，并对不同规模服务站设置每日补贴上限，本文将补贴收入纳入服务站年度收益测算。服务站年度利润由服务收入、政府补贴、服务直接支出、固定管理成本和建设折旧成本共同决定。为符合养老机构保本微利要求，模型设置利润率约束，使每个服务站年度利润率保持在0至8%之间。由于价格满意度呈分段变化，本文将各项服务价格离散化为平价、微溢价、中溢价和高溢价四类候选价格，并采用枚举搜索法求解最优定价组合。该方法计算过程透明，适用于本题小规模服务站定价优化场景。

服务可及性分析表明，补贴政策对不同类型老人的影响存在差异。自理老人服务需求较低，原始消费负担较轻，其可及性主要受服务距离和便利性影响；半失能老人开始出现专业照护需求，价格补贴可以有效提升其经济可及性；失能老人服务频次最高，对上门护理、助浴、康复理疗和紧急救助依赖更强，经济可及性和地理可及性均较弱，因此最需要政府补贴和近距离服务网络共同保障。

---

# 问题4：灵敏度分析与方案比较

## 4.1 参数变化下的重新求解

分别改变以下参数，重新求解问题2和问题3，并进行比较：

| 参数类型   | 参数变化                                                               |
| ---------- | ---------------------------------------------------------------------- |
| 老人增长率 | 60+老人年增长率调整为 **8%**                                           |
| 转移概率   | 自理→半失能转移概率调整为 **5.5%**，半失能→失能转移概率调整为 **9.5%** |
| 成本变化   | 日固定管理成本增加 **20%**                                             |
| 预算调整   | 总建设预算调整为 **140万元**                                           |

## 4.2 方案比较与敏感性分析

**任务**：

- 比较前后方案的变化，包括：
  - 站点数量与位置
  - 服务定价
  - 政府补贴总额
  - 覆盖率
  - 满意度等
- 分析模型对这些参数的敏感性
- 评价模型的鲁棒性

## 4.3 实际推广中的不确定因素分析

**任务**：

- 指出模型在实际推广中可能面临的至少3个其他不确定因素
- 给出应对策略建议

问题4可以作为全文的“稳健性检验与政策情景比较”部分。它的核心不是重新建立新模型，而是在问题1、问题2、问题3模型框架不变的情况下，改变关键参数，重新运行完整求解流程，然后比较最优方案是否发生明显变化。

可以将问题4理解为：

$$
\text{基准情景} \rightarrow \text{参数扰动情景} \rightarrow \text{重新求解} \rightarrow \text{指标比较} \rightarrow \text{敏感性与鲁棒性评价}
$$

---

## 4.4 问题4总体求解思路

问题4涉及四类参数变化：

| 情景 | 变化内容 | 影响环节 |
|---|---|---|
| 基准情景 | 使用原始参数 | 问题1、2、3 |
| 情景1：老人增长率提高 | 年新增老年人比例由7%变为8% | 影响老人数量预测、需求量、站点容量、利润与补贴 |
| 情景2：转移概率变化 | 自理→半失能变为5.5%，半失能→失能变为9.5% | 影响老人结构、服务需求结构、护理类需求 |
| 情景3：固定管理成本上升 | 日固定管理成本增加20% | 影响利润、定价、利润率约束 |
| 情景4：预算提高 | 建设预算由120万元变为140万元 | 影响可选站点数量、规模、覆盖率、满意度 |

对每个情景，都应重新执行完整求解流程：

$$
\text{老人数量预测}
\rightarrow
\text{消费约束需求预测}
\rightarrow
\text{服务站选址与规模优化}
\rightarrow
\text{服务定价与补贴优化}
\rightarrow
\text{指标汇总比较}
$$

其中，成本变化和预算调整不一定影响问题1的人口预测，但仍会影响问题2和问题3。

---

### 基准情景设置

先定义基准情景 $S_0$。

基准情景参数为：

$$
g=7\%
$$

$$
p_{12}=4.5\%
$$

$$
p_{23}=10\%
$$

$$
\text{建设预算}=120\text{万元}
$$

日固定管理成本为：

$$
G_S=2000,\quad G_M=3200,\quad G_L=4400
$$

服务站规模参数为：

| 规模 | 建设成本/万元 | 日固定管理成本/元 | 日最大服务人次 |
|---|---:|---:|---:|
| 小型 | 18 | 2000 | 1000 |
| 中型 | 32 | 3200 | 2000 |
| 大型 | 45 | 4400 | 3000 |

基准情景的求解结果作为后续对比对象。后面每个扰动情景都与 $S_0$ 比较。

---

### 情景1：老人增长率调整为8%

#### 参数变化

原模型中年均新增老年人比例为：

$$
g=7\%
$$

情景1调整为：

$$
g'=8\%
$$

自然死亡率仍为：

$$
d=5\%
$$

转移概率仍为：

$$
p_{12}=4.5\%,\quad p_{23}=10\%
$$

#### 人口递推模型变化

基准情景下：

$$
N_{i,1}(t+1)
=
0.95(1-p_{12})N_{i,1}(t)+0.07T_i(t)
$$

情景1下：

$$
N_{i,1}'(t+1)
=
0.95(1-p_{12})N_{i,1}'(t)+0.08T_i'(t)
$$

半失能和失能递推公式不直接改变：

$$
N_{i,2}'(t+1)
=
0.95p_{12}N_{i,1}'(t)+0.95(1-p_{23})N_{i,2}'(t)
$$

$$
N_{i,3}'(t+1)
=
0.95p_{23}N_{i,2}'(t)+0.95N_{i,3}'(t)
$$

#### 预期影响

老人增长率提高会增加第5年末老人总数，进而提高各小区服务需求总量。由于新增老人默认进入自理老人群体，短期内对助餐、日间照料和低强度康复理疗需求影响更明显；随着时间递推，自理老人中的一部分会继续转移为半失能老人，因此也会间接推高上门护理、助浴和紧急救助需求。

对问题2的影响主要体现在：

服务站总需求增加，部分原本容量利用率较高的站点可能接近或超过容量上限。若基准方案中某些站点利用率已处于 $0.85$ 以上，则情景1可能导致其响应满意度下降，甚至迫使模型选择更大规模站点或增加站点数量。

对问题3的影响主要体现在：

有效服务人次增加会提高服务收入和政府补贴，但也会提高直接支出。若补贴达到单站日补贴上限，则新增服务量不能完全获得补贴，服务站可能需要通过价格调整维持利润率约束。

---

### 情景2：转移概率调整

#### 参数变化

基准情景下：

$$
p_{12}=4.5\%,\quad p_{23}=10\%
$$

情景2调整为：

$$
p_{12}'=5.5\%
$$

$$
p_{23}'=9.5\%
$$

老人增长率仍为：

$$
g=7\%
$$

死亡率仍为：

$$
d=5\%
$$

#### 人口递推模型变化

情景2下状态转移矩阵变为：

$$
P_i'=
\begin{bmatrix}
1-0.055 & 0 & 0\\
0.055 & 1-0.095 & 0\\
0 & 0.095 & 1
\end{bmatrix}
$$

即：

$$
P_i'=
\begin{bmatrix}
0.945 & 0 & 0\\
0.055 & 0.905 & 0\\
0 & 0.095 & 1
\end{bmatrix}
$$

递推公式为：

$$
N_{i,1}'(t+1)
=
0.95(1-0.055)N_{i,1}'(t)+0.07T_i'(t)
$$

$$
N_{i,2}'(t+1)
=
0.95\times0.055N_{i,1}'(t)+0.95(1-0.095)N_{i,2}'(t)
$$

$$
N_{i,3}'(t+1)
=
0.95\times0.095N_{i,2}'(t)+0.95N_{i,3}'(t)
$$

整理为：

$$
N_{i,1}'(t+1)
=
0.89775N_{i,1}'(t)+0.07T_i'(t)
$$

$$
N_{i,2}'(t+1)
=
0.05225N_{i,1}'(t)+0.85975N_{i,2}'(t)
$$

$$
N_{i,3}'(t+1)
=
0.09025N_{i,2}'(t)+0.95N_{i,3}'(t)
$$

#### 预期影响

情景2中，自理老人转移为半失能老人的概率提高，半失能转移为失能的概率略微降低。综合效果通常表现为半失能老人数量上升更明显，失能老人数量变化可能较复杂。

对服务需求结构的影响是：

半失能老人需求占比提高，因此上门护理、康复理疗、助浴等中等强度服务需求会增加。由于半失能老人理论服务费用为822元/月，且部分低收入小区半失能老人会受到消费约束，因此需求增长并不会完全转化为有效消费需求。

对问题2的影响是：

服务需求类型结构会向照护类服务倾斜。如果后续模型只使用总服务人次容量，则影响主要体现为总需求增加；如果后续扩展为分服务类型容量，则情景2会显著影响护理人员、康复设备和助浴资源配置。

对问题3的影响是：

半失能老人服务需求增加会提高服务收入和补贴规模，但也会提高直接成本。若服务站覆盖低收入小区较多，消费约束与价格满意度约束会使提价空间有限。

---

### 情景3：日固定管理成本增加20%

#### 参数变化

基准日固定管理成本为：

$$
G_S=2000,\quad G_M=3200,\quad G_L=4400
$$

情景3中增加20%：

$$
G_S'=2000\times1.2=2400
$$

$$
G_M'=3200\times1.2=3840
$$

$$
G_L'=4400\times1.2=5280
$$

建设成本、容量和预算保持不变。

#### 年度固定成本变化

小型站年度固定管理成本由：

$$
365\times2000=730000
$$

增加为：

$$
365\times2400=876000
$$

中型站年度固定管理成本由：

$$
365\times3200=1168000
$$

增加为：

$$
365\times3840=1401600
$$

大型站年度固定管理成本由：

$$
365\times4400=1606000
$$

增加为：

$$
365\times5280=1927200
$$

#### 对问题2的影响

如果问题2目标函数只考虑覆盖率和满意度，而不把年度利润作为主要目标，则日固定管理成本上升对站点位置和规模的直接影响较弱。因为建设预算约束仍然基于一次性建设成本，而不是运营成本。

但如果问题2将年度利润作为分层目标中的第三层指标，或者将亏损风险纳入可行性约束，则固定成本上升会明显影响站点规模选择。模型可能倾向于减少站点数量，提高单站覆盖范围和利用率，以摊薄固定成本。

#### 对问题3的影响

成本上升对问题3影响非常直接。

利润率约束为：

$$
0\leq
\frac{R_j^{year}+B_j^{year}-C_j^{year}}{C_j^{year}}
\leq0.08
$$

固定成本上升导致：

$$
C_j^{year}
$$

增加。若服务价格不变，利润率下降，甚至可能变为负值。为了满足保本要求，模型可能需要提高部分服务价格，从而降低价格满意度 $S_3$，最终降低综合满意度。

因此，情景3通常会造成：

服务价格上升；

价格满意度下降；

年度利润率恢复到0至8%区间；

若补贴封顶，政府补贴无法完全抵消成本上升；

整体满意度下降。

---

### 情景4：建设预算调整为140万元

#### 参数变化

基准建设预算为：

$$
120\text{万元}
$$

情景4调整为：

$$
140\text{万元}
$$

其他参数保持不变。

#### 对问题2的影响

预算提高会扩大可行建站方案集合：

$$
\sum_{j,k}F_ky_{j,k}\leq120
$$

变为：

$$
\sum_{j,k}F_ky_{j,k}\leq140
$$

因此模型可能选择：

增加服务站数量；

将部分小型站升级为中型或大型站；

在边缘小区增设站点以提升距离满意度；

降低单站覆盖半径压力；

降低服务站利用率，提高响应满意度。

如果基准情景已经实现100%覆盖，则预算增加主要改善满意度和容量冗余，而不一定提高覆盖率。

如果基准情景存在未覆盖小区，预算增加可能直接提高覆盖率。

#### 对问题3的影响

预算提高本身不直接改变服务定价，但它会通过问题2改变站点布局和规模，从而影响：

距离满意度 $S_1$；

响应满意度 $S_2$；

单站服务需求规模；

政府补贴总额；

年度固定成本和折旧成本。

预算提高可能带来两种相反影响：

一方面，更多站点或更大规模站点可以降低距离和响应压力，提高满意度；

另一方面，站点数量增加会提高固定管理成本和折旧成本，可能导致定价压力上升。

因此，预算提高不一定必然改善价格满意度，但通常会改善地理可及性和响应满意度。

---

### 重新求解流程

对每个情景 $S_m$，执行如下步骤。

#### 步骤1：更新参数

根据情景修改对应参数：

$$
g,p_{12},p_{23},G_k,Budget
$$

#### 步骤2：重新预测第5年末老人数量

使用问题1.1递推模型重新计算：

$$
N_{i,k}^{(5,m)}
$$

其中 $m$ 表示情景编号。

#### 步骤3：重新计算消费约束需求

根据问题1.3模型，计算：

$$
D_{i,r}^{\ast,(m)}
$$

如果情景只改变成本或预算，不改变人口，则这一步结果与基准情景相同。

#### 步骤4：重新求解服务站选址与规模

使用问题2模型重新确定：

$$
y_{j,k}^{(m)},\quad x_{ij}^{(m)}
$$

得到站点数量、位置、规模和覆盖小区。

#### 步骤5：重新求解服务定价

使用问题3模型重新计算：

$$
p_{j,r}^{(m)}
$$

并得到年度利润、利润率、政府补贴和满意度。

#### 步骤6：比较评价指标

对每个情景输出：

$$
\text{站点数量}
$$

$$
\text{站点位置与规模}
$$

$$
\text{覆盖率}
$$

$$
\text{平均满意度}
$$

$$
\text{价格满意度}
$$

$$
\text{政府补贴总额}
$$

$$
\text{年度总利润}
$$

$$
\text{平均容量利用率}
$$

---

### 方案比较指标体系

建议建立如下对比表。

#### 站点布局变化表

| 情景 | 站点数量 | 站点位置 | 站点规模 | 总建设成本/万元 | 覆盖小区 |
|---|---:|---|---|---:|---|
| 基准情景 |  |  |  |  |  |
| 老人增长率8% |  |  |  |  |  |
| 转移概率变化 |  |  |  |  |  |
| 固定成本+20% |  |  |  |  |  |
| 预算140万元 |  |  |  |  |  |

---

#### 综合指标比较表

| 情景 | 覆盖率 | 全体老人加权满意度 | 平均价格满意度 | 政府补贴总额 | 年度总利润 | 平均容量利用率 |
|---|---:|---:|---:|---:|---:|---:|
| 基准情景 |  |  |  |  |  |  |
| 老人增长率8% |  |  |  |  |  |  |
| 转移概率变化 |  |  |  |  |  |  |
| 固定成本+20% |  |  |  |  |  |  |
| 预算140万元 |  |  |  |  |  |  |

---

#### 服务定价变化表

| 情景 | 服务站 | 助餐 | 日间照料 | 上门护理 | 康复理疗 | 助浴 | 紧急救助 |
|---|---|---:|---:|---:|---:|---:|---:|
| 基准情景 | 站点1 |  |  |  |  |  | 0 |
| 老人增长率8% | 站点1 |  |  |  |  |  | 0 |
| 转移概率变化 | 站点1 |  |  |  |  |  | 0 |
| 固定成本+20% | 站点1 |  |  |  |  |  | 0 |
| 预算140万元 | 站点1 |  |  |  |  |  | 0 |

---

#### 小区满意度变化表

| 小区 | 基准情景 | 老人增长率8% | 转移概率变化 | 固定成本+20% | 预算140万元 |
|---|---:|---:|---:|---:|---:|
| A |  |  |  |  |  |
| B |  |  |  |  |  |
| C |  |  |  |  |  |
| D |  |  |  |  |  |
| E |  |  |  |  |  |
| F |  |  |  |  |  |
| G |  |  |  |  |  |
| H |  |  |  |  |  |
| I |  |  |  |  |  |
| J |  |  |  |  |  |

---

### 敏感性指标设计

为了定量分析敏感性，可以定义相对变化率。

设某指标 $Y$ 在基准情景下的值为：

$$
Y_0
$$

在扰动情景 $m$ 下的值为：

$$
Y_m
$$

则指标变化率为：

$$
\Delta Y_m=
\frac{Y_m-Y_0}{Y_0}\times100\%
$$

例如：

覆盖率变化率：

$$
\Delta CR_m=
\frac{CR_m-CR_0}{CR_0}\times100\%
$$

满意度变化率：

$$
\Delta \bar{S}_m=
\frac{\bar{S}_m-\bar{S}_0}{\bar{S}_0}\times100\%
$$

政府补贴变化率：

$$
\Delta B_m=
\frac{B_m-B_0}{B_0}\times100\%
$$

年度总利润变化率：

$$
\Delta \Pi_m=
\frac{\Pi_m-\Pi_0}{\Pi_0}\times100\%
$$

如果参数变化幅度已知，还可以定义敏感性系数：

$$
E_Y=
\frac{\Delta Y/Y_0}{\Delta \theta/\theta_0}
$$

其中 $\theta$ 是被扰动的参数。

例如，老人增长率从7%变为8%，参数相对变化为：

$$
\frac{8\%-7\%}{7\%}=14.29\%
$$

若覆盖率从95%变为100%，则覆盖率相对变化为：

$$
\frac{100\%-95\%}{95\%}=5.26\%
$$

则覆盖率对老人增长率的敏感性系数为：

$$
E_{CR}=\frac{5.26\%}{14.29\%}=0.368
$$

一般可以按以下标准解释：

| 敏感性系数绝对值 | 解释 |
|---:|---|
| $<0.3$ | 不敏感 |
| $0.3\sim0.8$ | 中等敏感 |
| $>0.8$ | 高敏感 |

---

### 各参数敏感性预期分析

#### 对老人增长率的敏感性

老人增长率直接影响老人总数和服务需求规模。若原选址方案容量冗余较大，则老人增长率变化可能只提高容量利用率，对站点布局影响较小。若原方案容量利用率已经较高，则增长率上升会导致响应满意度下降，并可能促使模型增加站点或扩大规模。

预期结论：

老人增长率对服务需求量和容量利用率较敏感；

对站点位置的影响取决于基准方案的容量冗余；

对服务定价有间接影响，主要通过收入、补贴和成本规模体现。

---

#### 对转移概率的敏感性

转移概率主要影响老人类型结构，而不是单纯影响老人总量。自理转半失能概率提高，会增加半失能老人比例；半失能转失能概率降低，会使部分老人停留在半失能状态。

预期结论：

转移概率对服务结构敏感，尤其影响上门护理、康复理疗、助浴等服务；

对总覆盖率影响通常弱于老人增长率；

对价格与补贴有中等影响，因为半失能和失能服务的单次价格、直接成本较高；

如果模型扩展为分服务类型容量约束，则转移概率的影响会明显增强。

---

#### 对固定管理成本的敏感性

固定管理成本不改变人口和需求，也不改变服务半径，因此它对覆盖率的直接影响较小。但它会显著影响年度成本、利润和定价。

预期结论：

固定管理成本对服务定价和利润率高度敏感；

对价格满意度有明显负向影响；

对覆盖率影响较弱；

对年度利润和保本可行性影响最大。

---

#### 对建设预算的敏感性

建设预算直接改变可行站点方案集合。若基准预算是主要约束，预算提高会显著改善覆盖率、距离满意度和响应满意度。若基准预算已经足以覆盖主要区域，则预算提高的边际改善较小。

预期结论：

建设预算对站点数量、规模和地理满意度敏感；

对服务定价影响间接，可能因固定成本和折旧成本增加而出现不确定变化；

若覆盖率已达到100%，预算提升主要改善满意度而不是覆盖率；

模型对预算变化的敏感性取决于基准方案是否存在未覆盖小区或高利用率站点。

---

### 鲁棒性评价方法

模型鲁棒性可以从三个层面评价。

#### 布局鲁棒性

如果参数变化后，最优站点位置大体保持不变，只是部分规模调整，则说明选址方案具有较好鲁棒性。

例如：

基准方案选择 B、E、H 建站；

老人增长率提高后仍选择 B、E、H，只是将 B 从小型升级为中型；

这种情况说明核心站点位置稳定，模型对人口扰动不敏感。

如果参数轻微变化导致站点位置大幅改变，则说明模型对参数敏感，实际建设中应保留弹性空间。

---

#### 服务能力鲁棒性

若参数变化后，各站点容量利用率仍保持在合理区间，例如：

$$
0.60\leq u_j\leq0.85
$$

则说明服务能力具有缓冲空间。

若多个站点利用率接近：

$$
u_j>0.95
$$

则说明方案缺乏容量冗余，对需求增长不稳健。

---

#### 财务鲁棒性

若成本上升或补贴变化后，服务站仍能在不大幅提价的条件下满足：

$$
0\leq \rho_j\leq8\%
$$

则说明方案财务鲁棒性较好。

若固定成本上升20%后，服务站必须进入高溢价区间，导致价格满意度明显下降，则说明方案对运营成本高度敏感。

---

### 实际推广中的其他不确定因素

题目要求至少指出3个其他不确定因素，并给出应对策略。可以写以下几点。

---

#### 实际需求与预测需求存在偏差

模型使用平均服务需求次数预测老人服务需求，但实际需求会受到健康状况、家庭照护、季节变化、突发疾病、老人主观意愿等因素影响。部分老人即使有服务需求，也可能因习惯、认知或家庭支持而不使用服务。

应对策略：

建立动态需求监测机制。服务站运行后持续收集预约记录、服务记录、退单记录和满意度反馈，每季度校准需求参数。对高频使用服务和低频使用服务分别建立修正系数，使模型从静态预测逐步转向滚动预测。

---

#### 人工成本和物价波动

养老服务高度依赖护理员、康复师、助浴人员和餐饮配送人员。若工资水平、餐食原料、能源费用和租金上涨，服务站运营成本会高于模型预测，导致原定价格和补贴不足以维持保本运营。

应对策略：

建立成本指数联动机制。将人员工资、食品价格、能源价格纳入年度成本调整公式，并设置政府补贴动态调整机制。当关键成本指数超过阈值时，自动调整补贴标准或允许价格小幅调整。

---

#### 服务人员供给不足

即使服务站建设完成，如果缺乏足够的护理人员、康复理疗人员和应急响应人员，实际服务能力也无法达到设计容量。模型中用“日最大服务人次”表示服务能力，但现实中服务能力往往受人员排班、资质和流动率限制。

应对策略：

将人员配置纳入后续优化模型。对不同服务设置最低人员配比，如护理服务、康复理疗、助浴服务和紧急救助分别设置专业人员需求。建立社区养老服务人员储备库，并通过培训补贴和岗位激励提高人员稳定性。

---

#### 道路通行与空间距离不一致

附件4使用小区间距离矩阵，但现实中的服务可达性取决于道路网络、步行环境、电梯条件、交通管制、坡道、无障碍设施等因素。直线距离或简化距离可能低估真实到达时间。

应对策略：

在实际推广中引入GIS路网数据，用步行时间、骑行时间或上门服务通行时间替代简单距离。对于行动不便老人较多的小区，应额外考虑楼栋密度、电梯覆盖率和无障碍通道情况。

---

#### 政策补贴标准变化

政府补贴政策可能随财政状况、人口老龄化压力和服务绩效考核发生变化。如果补贴标准降低或补贴上限收紧，服务站利润率和定价方案会受到直接影响。

应对策略：

建立多补贴情景测算机制。在服务站建设前分别测算高补贴、中补贴和低补贴情景下的财务可持续性。对公益性强但盈利能力弱的服务，如紧急救助和助浴服务，设置专项补贴或购买服务合同。

---

#### 老人对价格和服务质量的敏感性存在差异

模型中价格满意度采用统一分段规则，但现实中不同收入、健康状态和家庭支持程度的老人对价格变化的敏感程度不同。低收入失能老人可能对价格更敏感，高收入自理老人可能更重视服务品质和便利性。

应对策略：

引入分类型价格敏感系数。对自理、半失能、失能老人分别建立价格弹性参数，并结合收入水平进行修正。后续可采用问卷调查或历史消费数据估计价格弹性，使定价模型更贴近真实行为。

---

## 4.5 问题4可写入报告的完整表述

可以直接使用下面这段作为问题4正文基础：

为检验模型在关键参数变化下的稳定性，本文在基准情景基础上设置四类扰动情景，分别考察老人增长率、健康状态转移概率、服务站固定管理成本和建设预算变化对选址、规模配置、定价、补贴和满意度的影响。各情景均沿用前文建立的人口预测模型、消费约束需求模型、服务站选址模型和定价补贴优化模型，仅改变相应参数并重新求解。通过比较站点数量与位置、服务定价、政府补贴总额、覆盖率、满意度和利润率等指标，分析模型对不同参数的敏感性。

从理论影响路径看，老人增长率主要影响服务需求规模和容量利用率；转移概率主要影响老人健康结构和服务需求类型；固定管理成本主要影响定价、利润率和价格满意度；建设预算主要影响站点数量、空间覆盖和距离满意度。若参数扰动后站点核心位置保持稳定、覆盖率和满意度变化较小、服务站仍能满足利润率约束，则说明模型具有较好的鲁棒性。若某类参数变化导致站点布局大幅调整或价格满意度显著下降，则说明该参数是模型中的关键敏感因素，需要在实际建设前进行更细致的数据校准和情景测算。

在实际推广中，模型还可能受到真实需求偏差、人工成本波动、服务人员供给不足、路网可达性差异、补贴政策调整和老人价格敏感性差异等不确定因素影响。为降低这些因素带来的风险，应建立动态数据更新机制、成本指数联动机制、人员配置约束、GIS路网修正模型和多补贴情景测算机制，使服务站规划方案能够在长期运营中保持稳定性和可调整性。
